import { SettingsPage } from "@/features/auth/components/SettingsPage";

/** StudentSettingsPage presents account sections with persistent local form state. */
export default function StudentSettingsPage() {
  return <SettingsPage base="/app/settings" />;
}
