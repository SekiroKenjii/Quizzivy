import { SettingsPage } from "@/features/auth/components/SettingsPage";

/** AdminSettingsPage presents account sections with persistent local form state. */
export default function AdminSettingsPage() {
  return <SettingsPage base="/admin/settings" />;
}
