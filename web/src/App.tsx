/**
 * Placeholder shell. The router, the three route trees and the four layouts
 * arrive in T-0.10; this exists so the build has an entry point and the design
 * tokens are visible somewhere.
 */
export default function App() {
  return (
    <main className="mx-auto flex min-h-svh max-w-md flex-col justify-center gap-4 p-6">
      <h1 className="text-2xl font-semibold tracking-tight">Quizzivy</h1>
      <p className="text-muted-foreground text-sm">
        Scaffold hoạt động. Router và các layout sẽ có ở T-0.10.
      </p>
    </main>
  );
}
