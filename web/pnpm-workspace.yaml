# pnpm 11 requires explicit approval for postinstall scripts and reads it here,
# not from package.json. esbuild's postinstall places the platform binary that
# Vite's bundler depends on.
allowBuilds:
  esbuild: true
