# Backend

Go module `quizzivy` (spec §4) is scaffolded in **T-0.14**. `internal/` mirrors
the frontend feature folders so a vertical slice is one folder on each side.

`gen/openapi/` is generated from `api/openapi.yaml` and committed. Never edit it
by hand.
