# R2 — Access (v0.8.0)

R2 turns a one-admin application into a multi-teacher one and changes no
screen. It does six things:

- stores the permission catalogue and the built-in roles as data
  (`70-redesign-overview.md` §4.1);
- enforces one declared permission on every operation, driven by the contract;
- gives every class, test, question, question group and media asset an owner,
  and scopes every teacher read and write by that owner;
- moves the teaching operations from `/admin/*` to `/teacher/*`;
- stores new join codes encrypted, found through a keyed lookup;
- raises the join and sign-in rate limits so a whole class can sign in from
  one school address.

It ships alone (70 §2 rule 3), so any regression between v0.7.0 and v0.8.0 is
an access bug. At release, production has one staff account and its students.
The owner's `role='admin'` maps to the built-in Admin role; students map to the
built-in Student role. No UI can create a second teacher until R5's Users page
(rule 2). Until then, three things prove the multi-teacher behaviour instead of
production traffic: the seeded second teacher, the two-teacher isolation suite
and the escalation tests.

The Assistant role exists with the deck's grants. It owns nothing and is
attached to no class until DG-04 is drawn, so an assistant reaches the teacher
workspace and finds it empty. No role picker offers the role yet (D14).

Every change here is the expand half. The contract half belongs to R3, or to
whichever release ships next (70 §2 rule 4). That release:

- validates the `NOT NULL … NOT VALID` constraints and drops the fill triggers;
- drops `users.role`, its sync trigger, `users_role_active_idx` and
  `app.user_role`;
- removes the legacy `/admin` alias;
- stops issuing the legacy `role` claim.

Nothing in R2 may rely on those objects being gone, and nothing R2 writes may
rely on them staying.

Order of work: T-R2.1 to T-R2.8 build the permission model and the path move.
T-R2.9 to T-R2.13 add ownership, scoping and the guards that are not
permissions. T-R2.14 and T-R2.15 cover join codes and rate limits. T-R2.16 to
T-R2.19 prove the release, align the web and update the docs. The join-code
work (T-R2.14a) and the ownership migrations (T-R2.9) do not depend on each
other and can run beside T-R2.3 to T-R2.8.

**Deliverable:**

- **Permissions.** Roles and the permission catalogue are data. Every one of
  the 104 operations (103 today plus `getJoinCode`) declares `x-permission`,
  or is open. `httpx.RequirePermission` replaces the `/admin/` prefix gate.
  A short per-user principal cache supplies each request's role, permissions,
  `disabled_at` and session epoch.
- **Ownership.** Classes, tests, questions, question groups and media assets
  have owners. Repositories filter by `access.Scope`, and another teacher's
  ids answer 404.
- **Paths.** Teaching operations live under `/teacher/*`. An outer alias
  serves the old paths for one release, and every rate-limit key has moved.
- **Join codes.** New codes are stored with AES-256-GCM and found by a keyed
  HMAC. Legacy codes still redeem. The key can be rotated, and the procedure
  is documented.
- **Guards that are not permissions.** Strict student targets, the subset
  rule, the last Admin, and foreign-key refusals answered as 409.
- **Rate limits.** Join and sign-in limits are sized for a classroom.
- **Web.** Guards read permissions instead of a role, with no visual change.

**Exit criteria:**

- **Contract checks hold.**
  - The startup assertion, `permissions_test.go` and the web contract test
    agree: every operation declares a permission, the open operations declare
    none, and every key is in the catalogue.
  - `RequireRole` is gone.
- **Isolation and escalation tests are green.**
  - The isolation suite shows teacher B getting 404 for every `/teacher/*`
    operation aimed at teacher A's data, and never seeing A's rows in a list;
    student B gets the same on `/app/*`.
  - The escalation tests are green, including an Admin with "Take tests"
    turned on as the target.
- **Migrations are safe to roll out.**
  - The old-binary insert test is green.
  - Migrations pass up, down, up.
  - The Neon rehearsal on a production branch is recorded.
- **The rest of CI and the live join test pass.**
  - Every CI step passes locally, in order (70 §8).
  - The live test in which thirty students join from one address is green.
- **v0.8.0 runs in production with nothing visibly changed.**
  - The owner's daily work is unchanged, and so is a student's.
  - A legacy join code redeems.
  - Tabs still open on v0.7.0 keep working through the alias.

**Decisions taken in this file, worth a second look.**

- **`x-permission` is a key, a pseudo-key, or a list meaning "any of".**
  70 §4.2 gives each operation one key. Three families of operations need two:
  - Question and question-group writes serve both the bank
    (`content.questions.write`) and the test builder (`content.tests.write`).
  - The attempt-review reads serve both graders and teachers who intervene.

  The middleware accepts any listed key. When the body decides which key
  applies (a bank destination or a section), the handler narrows the check.
  Handlers already own rules the schema cannot express. The alternative,
  `workspace.teacher` on those writes, would let a role that only takes
  attendance edit the bank. 70 §4.2 states this form and names the families
  that use a list.
- **Reads declare `workspace.teacher`; matrix rows gate actions and student
  data.** The deck's matrix has no "view tests" row. Listing tests, questions,
  groups, media, classes and assignments, and opening the dashboard, needs any
  teacher key. Student data needs `people.students.read` or a teaching key.
  Ownership scoping, not the permission, keeps another teacher's rows out.
- **`scope.all` lifts every owner filter, in lists as well as by id.** The
  owner owns everything today. Filtering their Students list to "my classes or
  created by me" would hide students who belong to no class and have a NULL
  `created_by`, and hiding them is a visible change. R4 decides the Admin's
  teacher-workspace lists (DG-53). The proposal for 70 §4.1: `scope.all`
  keeps reads and edits by id and the Admin console's cross-teacher views,
  while content lists in the teacher workspace (tests, bank, media, imports,
  assignments) show the caller's own rows, admins included; Students and
  Classes keep `scope.all` lists until R5's Admin Classes and Users take them
  over. Either answer is the `Scope` a list query receives, so R2 builds
  nothing that R4 must undo.
- **The subset rule ignores `learning.take_tests`.** 70 §4.3 requires the
  target's permissions to be a subset of the actor's. Read literally, no Admin
  could disable a student, because the Admin wildcard lacks "Take tests" unless
  it is turned on (DG-51). That key gives no power over another person, so the
  rule compares the target's set without it. Admin targets stay protected,
  because the wildcard holds every other key. 70 §4.3 states the rule this
  way; Thuong confirms this reading of D13 (open items).
- **One database view, `app.student_like_roles`, is the strict student
  predicate.** It holds the built-in Student role, plus any custom role
  (`builtin_key IS NULL`) whose grants are a subset of
  `{learning.take_tests}`. Every repository and `core/maintenance` use it. The
  Admin row stores only its "Take tests" cell, so a definition based only on
  grants would classify the Admin as a student. That is the takeover the
  critic found; excluding built-in roles by key closes it. There is one
  definition and one test.
- **`deleteStudent` becomes `DELETE /admin/users/{id}` (`deleteUser`,
  `people.users.manage`) and stays student-only in R2.** Deleting an account
  is a People-row action, not a teaching one. Under `/teacher/*` it would
  break the path-tree rule (T-R2.7). R5 keeps it student-only, with no
  console control: offboarding is disable, then transfer.
- **A teacher resets only a student no other teacher shares.** A student is
  visible to every teacher whose class they belong to (T-R2.12d), so without
  this rule teacher B could reset a student shared with teacher A, receive the
  temporary password and sign in as the student. `resetStudentPassword`
  therefore requires every active membership of the student to be in a class
  the actor teaches, or, with no membership, `created_by` = the actor.
  Anything else answers `403 STUDENT_SHARED`, and the reset goes to a holder
  of `people.users.manage` (T-R2.13). In R2 the only staff account is the
  Admin, which holds that key, so nothing visible changes.
- **`updateStudent` with `disabled` also needs `people.users.manage`.** The
  operation itself declares `people.students.create`. "Add and disable users"
  is Admin-only in the matrix, and the deck's teacher Students page has no
  Disable.
- **A test draft's references keep one owner.** A draft may reference only
  bank questions owned by the test's owner. The owner is checked after
  `questions.LockForDraftUse`, which stays the lock the canary pins. A
  duplicate or a restored draft belongs to the source test's owner. The cost
  in R2: an Admin editing another teacher's draft can pick only that teacher's
  questions. R7's copy-on-add removes that limit. Media works differently:
  assets are immutable, so a reference only requires that the actor can read
  the asset.
- **Principals are cached for 10 seconds.** A write on the same machine
  forgets the entry at once. A disable or a matrix edit therefore applies on
  the next request on that machine, and within 10 seconds on any other. This
  keeps autosave and the monitor's polling from adding a query per request,
  which would keep Neon awake (PR-7).
- **In R2 the session epoch moves only on disable and on a staff password
  reset.** Those are the only R2 operations that must end live sessions at
  once. R5's "Sign out everywhere" and role changes will also move it.
- **The last-admin guard is a trigger now, although no R2 operation can reach
  it.** `users_last_admin` serialises on the Admin role row and refuses to
  leave zero active Admins, whatever the path: R5's commands, a bulk action or
  a hand-written `UPDATE`. Its function is `SECURITY DEFINER`, because the
  `FOR UPDATE` it takes needs UPDATE privilege on `app.roles`, which T-R2.1
  revokes from the app role. The `409 LAST_ADMIN` code joins the contract with
  the first operation that can return it (R5).
- **The API can read a full join code in R2; the screen that shows it comes in
  R4.**
  - `GET /teacher/classes/{id}/join-code` returns `{code, legacy}`.
  - A legacy row answers `code: null, legacy: true`, the explicit state the
    critic asked for.
  - R4 is then only UI plus the rotation of legacy codes (D5).
  - The key id is a 16-bit HKDF fingerprint of the key rather than a number an
    operator picks, so a rotation cannot reuse an id by mistake.
- **Built-in role names and descriptions are seeded in Vietnamese**: Quản trị
  viên, Giáo viên, Trợ giảng, Học viên. They are data in the organisation's
  language and editable later (D2). R5 renders a built-in role whose stored
  name is still the seeded one from `role.builtin.<key>`, so an English matrix
  reads "Teacher". Thuong reviews the four rows in T-R2.1 (D11).
- **The rate-limit numbers in T-R2.15 are proposals.**
  - 60 requests a minute per address for join preview, join and sign-in.
  - A bucket per address and code, so one device cannot hammer a code.
  - 200 an hour per code.

  A class of 40 with three tries each fits. Guessing stays at or under 600
  attempts an hour per address, against 32⁸ possible codes.

---

### T-R2.1 — Permission catalogue and built-in roles
**Depends on:** —
**Touches:**
- `migrations/NNNNN_create_roles_and_permissions.sql` (new)
- `migrations/NNNNN_create_student_like_roles_view.sql` (new)
- `api/openapi.yaml` (`PermissionKey`)
- `server/gen/openapi/`, `web/src/lib/api/schema.d.ts`
- `server/internal/shared/access/` (new): `key.go`, `set.go`,
  `requirement.go`, `scope.go`, `principal.go`
- `server/internal/shared/access/tests/` (new)
- `server/internal/core/tests/catalogue_test.go` (new)
- `docs/plan/20-data-model.md`

**Size:** M
**Done when:**
- [ ] `create_roles_and_permissions` creates `app.permissions`:
  - Columns: `key text PRIMARY KEY CHECK (key ~ '^[a-z]+(\.[a-z_]+)+$')`,
    `group_key text NOT NULL CHECK (group_key IN
    ('content','teaching','people','system'))`,
    `ordinal smallint NOT NULL CHECK (ordinal > 0)`,
    `in_matrix boolean NOT NULL`, and `UNIQUE (group_key, ordinal)`.
  - Rows: the eighteen matrix rows of 70 §4.1, in the deck's order
    (`Quizzivy Admin.dc.html`, `PERMS`), with `people.students.create` second
    in People (DG-03).
  - Rows: the four hidden keys (`scope.all`, `system.api_reference`,
    `system.data_export`, `system.leads`) with `in_matrix = false`.
- [ ] The same file creates `app.roles`:
  - Columns:
    - `id uuid PRIMARY KEY DEFAULT uuidv7()`
    - `builtin_key text UNIQUE CHECK (builtin_key IN
      ('admin','teacher','assistant','student'))`
    - `name text NOT NULL CHECK (length(btrim(name)) BETWEEN 1 AND 60)`
    - `description text NOT NULL DEFAULT '' CHECK (length(description) <= 300)`
    - `icon text NOT NULL`, checked against the Edit role dialog's twelve
      icons: shield, graduation-cap, hand-helping, user, key-round, crown,
      briefcase, book-open, users, clipboard-check, headset, star
    - `color text NOT NULL`, checked against the seven card colours: dark,
      lime, blue, green, amber, rose, gray
    - `copied_from uuid REFERENCES app.roles ON DELETE SET NULL`
    - `revision bigint NOT NULL DEFAULT 1 CHECK (revision > 0)`
    - `created_by uuid REFERENCES app.users ON DELETE SET NULL`
    - `created_at timestamptz NOT NULL DEFAULT now()`
    - `updated_at timestamptz NOT NULL DEFAULT now()`
  - Indexes and triggers: `UNIQUE INDEX roles_name_lower_key ON app.roles
    (lower(name))`, and the `roles_set_updated_at` trigger.
- [ ] The same file creates `app.role_permissions (role_id uuid REFERENCES
      app.roles ON DELETE CASCADE, permission_key text REFERENCES
      app.permissions ON DELETE RESTRICT, PRIMARY KEY (role_id,
      permission_key))`, with an index on `(permission_key)` for the foreign
      key.
- [ ] Reference rows: the four built-in roles, with the deck's default cards
      and names written in Vietnamese.

      | Role | Icon | Colour | Name | Description | Grants |
      |---|---|---|---|---|---|
      | Admin | shield | dark | Quản trị viên | "Điều hành trung tâm. Quản lý người dùng và cài đặt." | none stored (wildcard; "Take tests" off) |
      | Teacher | graduation-cap | lime | Giáo viên | "Soạn đề, quản lý lớp, chấm bài." | 13 (70 §4.1) |
      | Assistant | hand-helping | blue | Trợ giảng | "Hỗ trợ giáo viên chấm bài và điểm danh." | 6 |
      | Student | user | gray | Học viên | "Làm bài và xem kết quả của mình." | `learning.take_tests` |

      Ids come from `uuidv7()`. Code and seeds always resolve a role by
      `builtin_key`.
- [ ] Triggers:
  - `roles_builtin_key_immutable` (BEFORE UPDATE).
  - `role_permissions_guard` (BEFORE INSERT): a key with
    `in_matrix = false` is never granted, and the Admin role may hold only
    `learning.take_tests`.
  - `role_permissions_keep_student_take_tests` (BEFORE DELETE): the built-in
    Student role cannot lose `learning.take_tests`.
  - `role_permissions_bump_revision` (AFTER INSERT OR DELETE, FOR EACH ROW):
    `roles.revision + 1` for the affected role. The table is tiny, and a
    statement-level trigger with transition tables would need one trigger per
    event. It runs as the invoker: in R2 only `quizzivy_migrate` writes
    grants, and from R5 the app role's UPDATE on `revision` covers it.
- [ ] Privileges: `REVOKE INSERT, UPDATE, DELETE` on the three tables from
      `quizzivy_app`, because `00009`'s default privileges grant them. R5's
      `grant_role_writes` (T-R5.8) grants the role commands' writes: INSERT
      and UPDATE (name, description, icon, color, revision, updated_at) on
      `roles`, INSERT and DELETE on `role_permissions`. DELETE on `roles` is
      never granted (DG-52), and `permissions` stays read-only.
- [ ] Down drops the triggers, the functions and the three tables.
- [ ] `create_student_like_roles_view` creates `app.student_like_roles (id)`:
      the roles with `builtin_key = 'student'`, or with
      `builtin_key IS NULL AND NOT EXISTS` a grant other than
      `learning.take_tests`.
  - `REVOKE INSERT, UPDATE, DELETE` on the view: a single-table view is
    automatically updatable, so the default privileges would otherwise let the
    app role delete roles through it.
  - Down drops the view.
- [ ] `api/openapi.yaml` gains `components.schemas.PermissionKey`, a string
      enum of the 22 keys, documented with its matrix row. T-R2.6 references
      it from `CurrentUser`. `make gen` output is its own commit.
- [ ] `shared/access` (kernel; imports nothing outside `shared/`) contains:
  - `Key` and constants for the 22 keys and the pseudo-keys `self`,
    `workspace.teacher` and `workspace.admin`.
  - `Set` with `Has`, `SubsetOf` and `Without`.
  - `Requirement` (any-of; pseudo-keys evaluated as in 70 §4.1) and
    `Workspaces(Set)`.
  - `IsStudentLike(builtinKey, Set)`.
  - `CanActOn(actor, target Set)`: the subset rule without
    `learning.take_tests`.
  - `Scope{UserID, All}` and `Principal{UserID, RoleID, BuiltinKey,
    Permissions, Epoch, Disabled}`.
- [ ] Unit tests in `shared/access/tests/`:
  - Every pseudo-key; `workspace.teacher` is true for the Assistant's six keys
    and false for `{learning.take_tests}`.
  - `CanActOn`:
    - true for Admin acting on a student;
    - false for a custom role holding `people.users.manage` acting on an Admin
      with "Take tests" turned on;
    - false for a Teacher acting on an Admin.
  - `IsStudentLike` is false for the Admin role with `learning.take_tests`
    granted, and true for an empty custom role.
- [ ] Integration, `core/tests/catalogue_test.go`:
  - The contract's `PermissionKey` enum equals `access.All()`, which equals
    `SELECT key FROM app.permissions`.
  - The built-in grants equal 70 §4.1, cell by cell.
  - The app role cannot write the three tables or the view.
  - Removing the Student's `learning.take_tests` raises; granting any role
    `scope.all` raises; granting the Admin role any other key raises.
  - A grant insert and a grant delete each bump `revision`.
- [ ] `20-data-model.md` gains a "Roles and permissions" section and a
      deviation at the next free D-number: reference data the app cannot run
      without lives in a migration (70 §3).
- [ ] up/down/up green; Thuong has reviewed the four role names and
      descriptions (D11).

---

### T-R2.2 — `users.role_id`, session epoch and the last-admin guard
**Depends on:** T-R2.1
**Touches:**
- `migrations/NNNNN_add_users_role_id.sql` (new)
- `migrations/NNNNN_index_users_role_id.sql` (new)
- `migrations/NNNNN_add_users_session_epoch.sql` (new)
- `migrations/NNNNN_add_users_last_admin_guard.sql` (new)
- `server/internal/modules/identity/repositories/store.go`
- `server/internal/modules/identity/repositories/students_write.go`
- `server/internal/modules/classes/repositories/enrol.go`
- `server/internal/platform/db/tests/expand_r2_test.go` (new)
- `docs/plan/20-data-model.md`

**Size:** M
**Done when:**
- [ ] `add_users_role_id`:
  - Adds `role_id uuid REFERENCES app.roles ON DELETE RESTRICT`, nullable.
  - Backfills `admin` → Admin and `student` → Student. The backfill runs with
    `users_set_updated_at` disabled, so `updated_at` keeps meaning "the user
    changed".
  - Raises if any row is still NULL.
  - `ALTER TABLE app.users ADD CONSTRAINT users_role_id_not_null NOT NULL
    role_id NOT VALID` (PG18, AGENTS).
- [ ] The same file adds the `users_sync_legacy_role` trigger, BEFORE INSERT
      OR UPDATE OF `role, role_id`:
  - An insert with no `role_id` takes it from `role`; this is how the old
    binary inserts.
  - An insert with `role_id` gets `role = 'student'` when the role is in
    `app.student_like_roles`, and `'admin'` otherwise. This overrides the
    column default.
  - An update of `role_id` re-derives `role`; an update of `role` alone
    re-derives `role_id`.
  - Down drops the trigger, the function, the constraint and the column.
- [ ] `index_users_role_id`: `-- +goose NO TRANSACTION`; `CREATE INDEX
      CONCURRENTLY users_role_id_active_idx ON app.users (role_id) WHERE
      disabled_at IS NULL`; Down is `DROP INDEX CONCURRENTLY IF EXISTS`.
- [ ] `add_users_session_epoch`: `session_epoch integer NOT NULL DEFAULT 0
      CONSTRAINT users_session_epoch_check CHECK (session_epoch >= 0)`. A
      constant default is metadata-only on PG18. Down drops the column.
- [ ] `add_users_last_admin_guard` adds the `users_last_admin` trigger, AFTER
      UPDATE OF `role_id, disabled_at` OR DELETE, FOR EACH ROW:
  - It acts when the old row was an active Admin and the new row is not.
  - It takes `FOR UPDATE` on the Admin row of `app.roles`, which serialises
    concurrent demotions.
  - It then raises `check_violation` with `CONSTRAINT = 'users_last_admin'`
    when no active Admin remains.
  - The function is `SECURITY DEFINER`, owned by `quizzivy_migrate`, with
    `SET search_path = app, pg_catalog`. `FOR UPDATE` needs UPDATE privilege
    on `app.roles`, which T-R2.1 revokes from `quizzivy_app`; as the invoker,
    R5's demote and disable would raise 42501 instead of the guard. It is the
    schema's first `SECURITY DEFINER` function, and `20-data-model.md`
    records why.
  - Down drops the trigger and its function.
- [ ] The new binary writes and reads `role_id`, never `role`:
  - `students_write.go` `Create` and `enrol.go`'s Google self-enrolment insert
    `role_id` as the built-in Student, resolved by `builtin_key` in the
    statement.
  - `store.go` (sign-in, Google, refresh) derives the legacy role value from
    `role_id` through `app.student_like_roles`, and selects `session_epoch`.
  - The remaining reads of `role` move in T-R2.12d and T-R2.13.
- [ ] `expand_r2_test.go` (integration) first sets up the legacy state:
  - `goose.UpTo` the version before `create_roles_and_permissions`, found by
    file name, because numbers are assigned at merge.
  - Inserts the legacy dataset the v0.7.0 way: two admins and three students,
    written by `role`.
  - Migrates the rest of the way up.
- [ ] It then asserts:
  - Every user has the right `role_id`, and no `updated_at` moved.
  - As the app role, an insert with `role` only gets `role_id`.
  - An insert with `role_id` set to Teacher gets `role = 'admin'`; set to
    Student, it gets `'student'`.
  - As the app role, demoting, disabling or deleting the last active Admin
    raises `users_last_admin`, not 42501.
  - When two transactions each disable one of the only two Admins, exactly one
    commits.
  - The `users_last_admin` function has `prosecdef`, its owner is
    `quizzivy_migrate`, its `proconfig` pins `search_path`, and
    `quizzivy_app` has no UPDATE on `app.roles`. Every other integration
    test connects as `quizzivy_migrate`, so these checks and the app-role
    cases above are what catch a missing privilege.
  - T-R2.9 and T-R2.14a extend the same test.
- [ ] up/down/up green; `20-data-model.md` records the four files.

---

### T-R2.3 — The `access` module: effective permissions and a cached principal
**Depends on:** T-R2.2
**Touches:**
- `server/internal/modules/access/domain/` (new)
- `server/internal/modules/access/application/{query,ports,model,internal/support}/`
  (new)
- `server/internal/modules/access/repositories/` (new)
- `server/internal/modules/access/*/tests/` (new)
- `server/internal/core/wiring/access.go` (new)
- `server/internal/core/wiring/wiring.go` (`Assembly.Principals`)
- `server/internal/core/tests/catalogue_test.go`

**Size:** L
**Done when:**
- [ ] Domain: `PermissionManager.Effective(role, grants, catalogue)` returns
      an `access.Set`.
  - The Admin gets every compiled key except `learning.take_tests`, and that
    key too when it is granted.
  - Every other role gets exactly its grants.
  - A key added by a later migration and compiled into a later binary reaches
    the Admin with no data change.
- [ ] Repositories: `Principal(ctx, userID)` is one statement: `SELECT
      u.role_id, r.builtin_key, r.revision, u.disabled_at IS NOT NULL,
      u.session_epoch FROM app.users u JOIN app.roles r ON r.id = u.role_id
      WHERE u.id = $1`.
  - `Grants(ctx, roleID)` and `Catalogue(ctx)` complete the read side.
  - No `SELECT *`.
- [ ] Application queries:
  - `ResolvePrincipal` returns an `access.Principal`; a missing user gives
    `ErrUnknownUser`.
  - `RolePermissions` is cached by `(role_id, revision)`.
  - `Matrix` returns the roles with their grants in catalogue order. The
    catalogue test reads the built-in grants through it; R5's Roles page
    extends it with member counts.
- [ ] The principal cache lives in `internal/support`: a 10-second TTL,
      10 000 entries, least-recently-used eviction, and `Forget(userID)` and
      `ForgetAll()` for writes on this machine.
- [ ] `wiring.Build` loads the catalogue and refuses to start when a compiled
      key is missing from `app.permissions`. Extra rows written by a newer
      migration are ignored, so an old machine still running during a later
      rollout keeps serving.
- [ ] Unit tests:
  - The wildcard, and the "Take tests" toggle.
  - TTL expiry with a fake clock; `Forget` forces a re-read; the entry bound
    holds.
  - A cache miss costs exactly one user query, plus one grants query per
    `(role, revision)`.
- [ ] Integration tests:
  - A disabled user resolves as `Disabled`.
  - A grant inserted as the migrate role bumps `revision`, and the next
    resolve after `ForgetAll` returns the new set.
  - The epoch is read.
- [ ] `core/tests/architecture_test.go` is green, and the package comment names
      the context's model.

---

### T-R2.4 — `x-permission` on every operation
**Depends on:** T-R2.1
**Touches:**
- `api/openapi.yaml`, `server/gen/openapi/`
- `server/internal/platform/httpx/permission_spec.go` (new)
- `server/internal/core/router/router.go`
- `server/internal/core/router/tests/permissions_test.go` (new)
- `server/internal/core/router/tests/testdata/permissions.golden` (new)
- `web/tests/units/contract/openapi-contract.test.ts`

**Size:** M
**Done when:**
- [ ] Every operation declares its permission as below, keyed by operationId.
      Paths move in T-R2.7; operationIds do not.

      | Operations | `x-permission` |
      |---|---|
      | previewJoinCode, login, googleAuth, refreshSession, logout, flushEvents (the beacon), getPublicStatus (R1) | none: open, `security: []` |
      | getCurrentUser, updateCurrentUser, changePassword, linkGoogle, unlinkGoogle | `self` |
      | listMyClasses, joinClass, listMyAssignments, getMyAssignment, startOrResumeAttempt, getAttempt, saveAnswers, recordAudioPlay, recordGroupAudioPlay, submitAttempt, getAttemptResult, getMediaUrl | `learning.take_tests` |
      | openDocsSession | `system.api_reference` |
      | deleteStudent (→ `deleteUser`, T-R2.7) | `people.users.manage` |
      | listQuestionGroups, getQuestionGroup, listTests, getTest, listTestVersions, previewTest, listQuestions, getQuestion, listMedia, listAssignments, getAssignment, getAssignmentMonitor, getDashboard, listClasses, getClass | `workspace.teacher` |
      | createQuestionGroup, updateQuestionGroup, deleteQuestionGroup, copyQuestionGroup | `[content.questions.write, content.tests.write]`; the handler requires the first for a bank group or destination and the second for a test section |
      | archiveQuestionGroup, deleteQuestion, tagQuestions | `content.questions.write` |
      | createQuestion, updateQuestion, duplicateQuestion | `[content.questions.write, content.tests.write]` |
      | createTest, updateTest, deleteTest, duplicateTest, createDraftFromTestVersion | `content.tests.write` |
      | publishTest, setCurrentTestVersion, deleteTestVersion | `content.tests.publish` |
      | createWordImport, listWordImports, getWordImport, uploadImportSource, downloadImportSource, getWordImportCapabilities, getWordImportLimits, processWordImport, cancelWordImport, getWordImportReview, saveWordImportReview, adoptWordImportReprocessed, getWordImportSource, commitWordImport | `content.tests.write` (DG-50) |
      | uploadMedia, deleteMedia | `content.media.write` |
      | createAssignment, updateAssignment, deleteAssignment, reopenAssignment | `teaching.assignments.write` |
      | listAnswersForQuestion, gradeAttempt, finishGrading, setAttemptNote, flagAttempt | `teaching.grading` |
      | listAttempts, getAttemptForReview, getAttemptEvents | `[teaching.grading, teaching.attempts.intervene]` |
      | extendAttempt, resetAttempt, voidAttempt | `teaching.attempts.intervene` |
      | listStudents, getStudent, listClassMembers | `people.students.read` |
      | createStudent, updateStudent | `people.students.create`; `disabled` additionally needs `people.users.manage` (T-R2.13) |
      | resetStudentPassword | `people.students.reset_password`; a student shared with another teacher's class additionally needs `people.users.manage` (T-R2.13) |
      | createClass, updateClass, deleteClass, addClassMember, removeClassMember, rotateJoinCode, revokeJoinCode | `teaching.classes.write` |

      Totals: 7 open, 5 `self`, 12 student, 2 admin and 77 teaching, which
      makes 103, the v0.7.0 contract. The open seven are R1's `theOpenSeven`
      pin, unchanged by R2.
- [ ] `info.description` states the three forms (key, pseudo-key, any-of
      list), what each pseudo-key means, and that open operations declare
      none.
- [ ] `httpx.PermissionRequirements(spec, catalogue)` builds a
      `map[pattern]access.Requirement`. It fails, listing every offender, when:
  - a non-open operation declares nothing, or an open one declares something;
  - a value is neither a catalogue key nor a pseudo-key;
  - a list is empty or repeats a key;
  - a hidden key appears outside `/admin/*`.
- [ ] `router.New` calls `PermissionRequirements` next to
      `AssertPublicRoutesLimited`, and the process refuses to start on an
      error.
- [ ] `permissions_test.go`:
  - `TestEveryNonOpenOperationDeclaresAPermission`,
    `TestOpenOperationsDeclareNone`, `TestEveryDeclaredValueIsKnown`.
  - `TestTheAssertionRefusesAnUndeclaredOperation` runs on a mutated copy of
    the spec.
  - `TestThePermissionMapIsPinned` compares against
    `testdata/permissions.golden`: one line per operation giving method, path
    and requirement, regenerated by `-update`. Every permission change then
    shows up as a diff in review.
- [ ] The web contract suite, which `make contract` runs, adds a test: every
      operation declares `x-permission` unless `security: []`, and every value
      is in `PermissionKey` or is a pseudo-key.
- [ ] `RequireRole` still gates in this PR. Nothing enforces the declarations
      until T-R2.5, so no commit on the branch is ever ungated.

---

### T-R2.5 — `RequirePermission` replaces the prefix gate
**Depends on:** T-R2.3, T-R2.4
**Touches:**
- `server/internal/platform/httpx/auth.go`
- `server/internal/platform/httpx/permission.go` (new)
- `server/internal/platform/httpx/tests/permission_test.go` (new)
- `server/internal/core/router/{router.go,server.go,deps.go}`
- `server/internal/core/core.go`, `server/internal/core/wiring/wiring.go`
- `server/internal/modules/identity/application/token/token.go`
- `server/internal/modules/identity/application/command/{login.go,google_sign_in.go,refresh.go,update_student.go,reset_student_password.go}`
- `server/internal/modules/identity/application/ports/ports.go`
  (`Principals.Forget`)
- `server/internal/core/adapters/principals.go` (new)
- `server/internal/modules/identity/repositories/students_write.go`
- `server/internal/modules/identity/http/docs.go`
- `server/internal/core/router/tests/{permissions_test.go,router_test.go,docs_gate_test.go,stubs_test.go}`
- `server/internal/core/router/tests/role_test.go` (removed, its cases carried
  over)
- `server/internal/modules/access/application/tests/revocation_test.go` (new)
- every Go test that mints a token (mechanical commit)

**Size:** L
**Done when:**
- [ ] Tokens:
  - `Claims` gains `Epoch int` as `sep`.
  - `Issue(userID, legacyRole string, epoch int)`.
  - A token without `sep` reads as epoch 0.
  - The legacy `role` claim is still issued (`student` for a student-like
    role, `admin` otherwise), so a v0.7.0 machine's `RequireRole` keeps
    working during the rolling deploy.
  - Sign-in, Google sign-in and refresh read `session_epoch` and put it in
    the token.
- [ ] `httpx.RequireAuth` puts `{UserID, Epoch}` in the context.
- [ ] `httpx.RequirePermission(requirements, resolve)`:
  - Open routes pass untouched.
  - Any other request with no principal gets 401.
  - An unknown user, a disabled user, or a token epoch older than the user's
    gets `401 UNAUTHORIZED` with `WWW-Authenticate`. The single-flight refresh
    settles it: refresh refuses the disabled user, and so the user is signed
    out.
  - An unmet requirement gets `403 FORBIDDEN`, with the existing Vietnamese
    message and no detail.
  - On success, the resolved `access.Principal` replaces the context value.
- [ ] The chain runs RateLimit → RequestMeta → RefreshCookie → RequireAuth →
      RequirePermission → body limit → validate. R1's maintenance middleware
      stays where R1 put it. `router_test.go` pins the whole order through
      `inExecutionOrder`.
- [ ] `RequireRole`, `RoleAdmin`, `AdminPathPrefix` and `IsAdminPattern` are
      deleted; `grep -rn "RequireRole\|IsAdminPattern\|RoleAdmin" server` is
      empty.
- [ ] `router.Deps` gains `Principals`, the access resolver, wired in
      `core.go`. `router.New` returns an error when it is nil.
- [ ] Docs session:
  - `OpenDocsSession` issues the docs token with the epoch.
  - `RequireDocsSession` resolves the cookie's subject through the same
    resolver, and requires `system.api_reference`, an active account and the
    current epoch: 401 and 403 as spec §5.5 describes.
- [ ] Epoch bumps: `updateStudent` with `disabled: true` and
      `resetStudentPassword` add 1 to `session_epoch` in their transaction,
      then call the `Principals.Forget` port. The student's live access token
      gets 401 on its next request.
- [ ] `permissions_test.go` uses a fake resolver with the built-in sets. From
      the spec, it generates:
  - A student gets 403 on every `/admin/*` operation, and on every
    `/teacher/*` one once T-R2.7 lands.
  - A Teacher gets 403 on `openDocsSession` and `deleteStudent`.
  - An Admin passes every staff operation, and gets 403 on every `/app/*`
    operation except the open beacon.
  - `self` operations pass for all four roles.
  - A disabled user gets 401; a stale epoch gets 401.
  - A revision bump in the fake changes the answer on the next request.
- [ ] `role_test.go`'s five tests move into `permissions_test.go` before the
      file is deleted, each as its permission-model equivalent. They are
      carried over, not dropped:
  - `TestAStudentCannotReachTheAdminTree`: a student gets 403 `FORBIDDEN` on
    the dashboard, a `workspace.teacher` operation.
  - `TestATeacherReachesTheAdminTree`: an Admin and a Teacher both reach the
    dashboard.
  - `TestEveryAdminOperationIsGated`: every non-open operation has an entry
    in the requirement map, with the same floor on how many it finds.
  - `TestTheStudentTreeIsNotGatedByRole`: the `/app` tree needs
    `learning.take_tests`; a student passes, and an Admin without "Take
    tests" gets 403.
  - `TestAnAnonymousCallerToTheAdminTreeGetsAuthenticationNotAuthorization`:
    an anonymous caller to a gated operation gets 401, never 403.
- [ ] `httpx/tests/permission_test.go` covers an open route with and without a
      token, any-of lists, each pseudo-key, and the resolved set in the
      context.
- [ ] `revocation_test.go` (integration) runs through the real router and
      database:
  - Disabling a user answers 401 on their next request.
  - A grant removed as the migrate role is refused after `ForgetAll`.
- [ ] A separate mechanical commit with no behaviour change: every Go test
      that mints a token uses the new `Issue` signature, and every test router
      passes a fake resolver.
- [ ] Canary `web/tests/units/api/client.refresh.test.ts` is untouched and
      green.

---

### T-R2.6 — The signed-in user carries permissions and workspaces
**Depends on:** T-R2.5
**Touches:**
- `api/openapi.yaml` (`CurrentUser`, `Workspace`)
- `server/gen/openapi/`, `web/src/lib/api/schema.d.ts`
- `server/internal/modules/identity/application/{query/current_user.go,ports/ports.go}`
- `server/internal/modules/identity/http/{auth.go,account.go,google.go}`
- `server/internal/core/wiring/identity.go`
- `server/internal/modules/identity/application/tests/me_test.go`
- `web/src/features/auth/api.ts`, `web/src/stores/auth.ts`
- `web/tests/support/fixtures.ts`, `web/tests/e2e/fixtures/`

**Size:** S
**Done when:**
- [ ] `CurrentUser` holds `User`'s properties plus two required fields:
  - `permissions: PermissionKey[]`: the effective set in catalogue order,
    including the hidden keys for the Admin.
  - `workspaces: Workspace[]`, from the `teacher`, `admin` and `app`
    workspaces, derived by `access.Workspaces`.
- [ ] `CurrentUser` is written flat, because a closed schema cannot be
      extended with `allOf` (issue #41).
- [ ] `CurrentUser` replaces `User` wherever the response describes the
      caller: getCurrentUser, updateCurrentUser, linkGoogle, `AuthSuccess` and
      `GoogleSignInSuccess`.
- [ ] `User` stays where it describes someone else: `getAttemptForReview`'s
      `student`. A student's permissions never appear in a teacher's payload.
- [ ] `role` keeps its two legacy values, derived from the role, so a tab
      still open on v0.7.0 keeps routing. The additions are ignored by the old
      SPA.
- [ ] `me_test.go`:

      | Role | `permissions` | `workspaces` |
      |---|---|---|
      | Admin | every key except `learning.take_tests` | `[teacher, admin]` |
      | Teacher | the 13 keys | `[teacher]` |
      | Assistant | the 6 keys | `[teacher]` |
      | Student | `[learning.take_tests]` | `[app]` |
- [ ] The web auth store and `features/auth/api.ts` use `CurrentUser`. The
      fixtures gain both fields in one mechanical commit, and
      `web/tests/units/contract/msw-contract.test.ts` is green.

---

### T-R2.7 — Teaching operations move to `/teacher/*`
**Depends on:** T-R2.5
**Touches:**
- `api/openapi.yaml` (paths, tags, descriptions)
- `server/gen/openapi/`, `web/src/lib/api/schema.d.ts`
- `server/internal/modules/identity/http/delete.go` (`DeleteUser`)
- `server/internal/core/router/ratelimits.go`
- `server/internal/platform/httpx/permission_spec.go` (tree rule)
- `server/internal/platform/httpx/hardening.go` (`/teacher/` gets
  `no-store`)
- `server/internal/core/router/tests/hardening_test.go`
- `server/internal/core/router/tests/credential_limits_test.go` (new)
- `server/internal/core/router/tests/permissions_test.go`
- `server/internal/core/router/tests/testdata/permissions.golden`
- the Go tests that name `/admin/` API paths (23 files today)
- the feature `api.ts` modules that call teaching operations (10 today)
- `web/src/lib/api/client.typecheck.ts`
- the web test files that stub API paths (about 55: MSW handlers, Playwright
  `page.route` stubs, live specs)

**Size:** L (most of it mechanical commits)
**Done when:**
- [ ] The 77 teaching operations move from `/admin/<path>` to
      `/teacher/<path>`, with operationIds unchanged so the Go and TypeScript
      names do not change.
- [ ] Every `admin-*` tag except `admin-docs` becomes `teacher-*`.
- [ ] Descriptions that say "admin" and mean the teacher are corrected, and
      `info.description` explains the split into `/teacher`, `/admin`, `/app`
      and `/auth`, with `/public` and `/join` for open operations and `/me`
      reserved for R4.
- [ ] `deleteStudent` becomes `DELETE /admin/users/{id}` (`deleteUser`, tag
      `admin-users`). Its handler is renamed; the command stays
      `DeleteStudent`, and the target stays strictly a student (T-R2.13).
- [ ] `openDocsSession` stays at `/admin/docs-session`.
- [ ] The path-tree rule is switched on in `PermissionRequirements`, with
      `TestPermissionTreesMatchPaths`:
  - `/teacher/*` accepts only `content.*`, `teaching.*`, `people.students.*`
    and `workspace.teacher`.
  - `/admin/*` accepts `people.users.manage`, `people.roles.manage`,
    `system.*`, `scope.all` and `workspace.admin`.
  - `/app/*` accepts `learning.take_tests`; the open beacon is exempt.
  - `/auth/*` accepts `self`; the open operations are exempt.
  - `/me/*` accepts only `self`. R4 creates the tree; the rule is in place
    before it.
  - `/public/*` and `/join/*` hold only open operations.
  - Open operations are exempt wherever their path lies: `/public/status`,
    `/join/preview`, the beacon and the four under `/auth/*`.
  - Any other prefix fails `PermissionRequirements` at startup.
  - `TestPermissionTreesMatchPaths` pins each rule, including an operation
    under an unknown prefix, a `/me/*` operation declaring a key, and a
    `/public/*` operation that is not open.
- [ ] Every `RateLimits()` key moves: `POST
      /teacher/students/{id}/reset-password` (5/min, 30/h, numbers unchanged).
      Two credential-minting operations that had no limit gain one:
  - `POST /teacher/students` (createStudent returns a temporary password):
    30/min and 300/h per address.
  - `POST /teacher/classes/{id}/join-code` (rotateJoinCode mints a bearer
    code): 10/min and 60/h.
  - These limits are per address, because the limiter runs before
    `RequireAuth` and no principal exists yet. In R2 the only staff account
    is the owner, so no centre NAT is shared. R4's principal-keyed limiter
    (`PrincipalRateLimits()`) is where the authenticated minters,
    createStudent, rotateJoinCode and getJoinCode, move to per-actor keys.
- [ ] `TestRegistryHasNoStaleEntries` is green.
- [ ] `credential_limits_test.go` pins `theCredentialMinters`: login,
      googleAuth, refreshSession, resetStudentPassword, createStudent,
      rotateJoinCode and openDocsSession. T-R2.14b adds getJoinCode.
  - Each has a registry entry.
  - An operation whose 2xx body carries `temporaryPassword` or `accessToken`
    at any depth, including inside arrays of objects
    (`items[].temporaryPassword`), or returns a `JoinCode` (its `code`), and
    is not on the list fails the test. The next credential-minting
    operation, a bulk one included, cannot ship without a limit.
- [ ] `SecurityHeaders` (`hardening.go`) sets `Cache-Control: no-store` on
      `/teacher/` as it does on `/auth/`, `/app/` and `/admin/`. Without it,
      createStudent's and resetStudentPassword's `temporaryPassword` and
      every student record become cacheable on the move. R4 adds `/me/`.
- [ ] `TestSecurityHeadersOnSuccessFailureAndPreflight` asserts `no-store` on
      one path per private tree (`/auth`, `/app`, `/teacher`, `/admin`), and
      that `/public/status` keeps its own `public, max-age=30`.
- [ ] Commits, in this order:
  1. contract
  2. `make gen`
  3. Go: the handler rename, the limits, the tree rule, the `no-store`
     prefix, the tests
  4. Go test path sweep (mechanical)
  5. web API-path sweep (mechanical)
- [ ] `pnpm typecheck` passing proves every call site matches the new schema.
- [ ] The web sweep changes API paths only: `api.ts` modules,
      `client.typecheck.ts`, and any MSW handler, Playwright route stub or live
      spec that addresses the API origin.
- [ ] Web routes (`to="/admin/…"`, `navigate("/admin/…")`) stay until R4.
- [ ] A grep in the PR shows nothing under `web/` addressing an API path under
      `/admin/` except `/admin/docs-session` and `/admin/users/{id}`.
- [ ] `permissions.golden` is regenerated. `make gen-check`, `pnpm typecheck`,
      `pnpm test:unit`, `pnpm test:integration` and `pnpm e2e` are green.

---

### T-R2.8 — The legacy `/admin` alias
**Depends on:** T-R2.7
**Touches:**
- `server/internal/core/router/legacy_admin.go` (new)
- `server/internal/core/router/legacy_admin_paths.go` (new)
- `server/internal/core/router/router.go`
- `server/internal/core/router/tests/legacy_admin_test.go` (new)

**Size:** M
**Done when:**
- [ ] The alias is an outer `http.Handler`, inside CORS and outside the
      generated handler. It has to be outside: the generated middlewares run
      only after the mux has matched a pattern, and an `/admin` path no longer
      in the spec never matches.
- [ ] Its own `ServeMux` holds the 78 pinned pairs: the 77 renames, plus
      `DELETE /admin/students/{id}` → `/admin/users/{id}`.
- [ ] A match rewrites `r.URL.Path`, clears `RawPath`, and logs
      `legacy_admin_path` at info with the method, the old pattern and the
      request id. The request then continues to the real mux, so auth,
      permission, rate limit and validation run exactly as they do on the new
      path.
- [ ] A request that matches no pair passes untouched.
      `POST /admin/docs-session` and `DELETE /admin/users/{id}` are never
      rewritten.
- [ ] `legacy_admin_test.go`:
  - The table equals a hard-coded list of the v0.7.0 `/admin` operations
    minus `openDocsSession`, so a later edit cannot shrink it silently.
  - For every pair, a student token gets the same status on the old path as
    on the new one (403), and an Admin token reaches the handler (never
    404 or 405).
  - The sixth reset-password in a minute through an old path gets 429,
    counted under the new key.
  - An `OPTIONS` preflight to an old path is answered as the new path's
    would be.
  - One log line per rewritten request.
- [ ] The release notes say the alias lasts one release; `73-r3.md` removes
      it.

---

### T-R2.9 — Ownership columns
**Depends on:** T-R2.2
**Touches:**
- `migrations/NNNNN_add_classes_teacher.sql` (new)
- `migrations/NNNNN_add_tests_owner.sql` (new)
- `migrations/NNNNN_add_questions_owner.sql` (new)
- `migrations/NNNNN_add_question_groups_owner.sql` (new)
- `migrations/NNNNN_add_media_assets_owner.sql` (new)
- `migrations/NNNNN_add_users_created_by.sql` (new)
- `server/internal/modules/classes/repositories/postgres.go` (Create)
- `server/internal/modules/identity/repositories/students_write.go`
- `server/internal/modules/tests/repositories/{store.go,duplicate.go,version_draft.go,group_create.go}`
- `server/internal/modules/questions/repositories/store.go`
- `server/internal/modules/media/repositories/store.go`
- the commands that carry the owner into those statements
- `server/internal/platform/db/tests/expand_r2_test.go`
- `docs/plan/20-data-model.md`

**Size:** L
**Done when:**
- [ ] `add_classes_teacher`:
  - `teacher_id uuid REFERENCES app.users ON DELETE RESTRICT`.
  - Backfilled from `classes.created_by`, which `00006` already declares
    `NOT NULL`. The oldest-admin rule `classes/repositories/lookup.go` applies
    today is not copied into the schema. In production the one staff account
    created every class, so the teacher shown today does not change.
  - Constraint `classes_teacher_id_not_null NOT NULL teacher_id NOT VALID`.
  - Trigger `classes_fill_teacher` (BEFORE INSERT) sets
    `NEW.teacher_id := NEW.created_by` when the old binary inserts without a
    teacher.
- [ ] `add_tests_owner`, `add_questions_owner` and
      `add_question_groups_owner`:
  - `owner_id uuid REFERENCES app.users ON DELETE RESTRICT`, backfilled
    from `created_by`.
  - Constraints `<table>_owner_id_not_null NOT NULL owner_id NOT VALID`.
  - A BEFORE INSERT trigger through `app.fill_owner_from_created_by()`, which
    the tests file creates and the other two reuse.
- [ ] `add_media_assets_owner` does the same from `uploaded_by`, through
      `app.fill_owner_from_uploaded_by()`.
- [ ] `add_users_created_by`: `created_by uuid REFERENCES app.users ON DELETE
      SET NULL`, nullable, with no backfill. A NULL means provenance is
      unknown, and a Google self-join has no creator.
- [ ] Each backfill disables the table's `*_set_updated_at` trigger for its
      `UPDATE`. Otherwise every test's `updated_at` would move, and an open
      builder tab's next autosave would get `STALE_WRITE` during the deploy.
- [ ] Each Down drops the trigger, the constraint and the column, and the
      function in the file that created it.
- [ ] The new binary sets the column explicitly; the triggers exist only for
      the old binary:

      | Row | Owner written |
      |---|---|
      | a class | the acting user |
      | a new test | the acting user |
      | a duplicated test, a restored draft | the source test's owner; `created_by` stays the actor |
      | a bank group | the acting user |
      | a section-owned group | the test's owner |
      | a bank question | the acting user |
      | a group member | the group's owner |
      | a restored-draft question | the test's owner |
      | a media asset | the uploader |
      | a student created by staff | `created_by` = the actor |
      | a Google self-join | `created_by` = NULL |
- [ ] `expand_r2_test.go` extended:
  - The legacy dataset backfills to the right owners: `created_by` for
    tests, questions, question groups and classes, `uploaded_by` for media.
  - No backfilled `updated_at` moved.
  - Old-binary inserts without owner columns are filled by the triggers.
- [ ] Duplicate and restore insert into `test_section_questions`, a high-risk
      path: every such insert still calls `questions.LockForDraftUse` first.
      The five canaries and the module's unit tests run on the branch point
      and on the last commit, both runs in the PR.
- [ ] Repository tests:
  - A Teacher's createClass stores that teacher, not the oldest Admin.
  - A committed Word import's test and questions belong to the importer.
- [ ] R3's validation will fail if any insert path forgets the column. Every
      insert path is exercised by an existing test, and R3 drops the triggers.
- [ ] `20-data-model.md` records the six files, with a deviation at the next
      free D-number: `owner_id` separate from `created_by`, and
      `classes.teacher_id` separate from `classes.created_by`. Provenance must
      survive R5's ownership transfer and R7's co-editing.
- [ ] up/down/up green.

---

### T-R2.10 — Ownership indexes
**Depends on:** T-R2.9
**Touches:**
- `migrations/NNNNN_index_classes_teacher.sql` (new)
- `migrations/NNNNN_index_tests_owner.sql` (new)
- `migrations/NNNNN_index_questions_owner.sql` (new)
- `migrations/NNNNN_index_question_groups_owner.sql` (new)
- `migrations/NNNNN_index_media_assets_owner.sql` (new)
- `migrations/NNNNN_index_users_created_by.sql` (new)
- `migrations/NNNNN_index_assignments_creator.sql` (new)
- `docs/setup/deploy.md`, `docs/plan/20-data-model.md`

**Size:** S
**Done when:**
- [ ] Each file is `-- +goose NO TRANSACTION` with a single `CREATE INDEX
      CONCURRENTLY`. Down is `DROP INDEX CONCURRENTLY IF EXISTS`. One index
      per file, so an interrupted build affects one index.

      | Index | Definition |
      |---|---|
      | `classes_teacher_idx` | `(teacher_id)` |
      | `tests_owner_idx` | `(owner_id, id DESC) WHERE deleted_at IS NULL` |
      | `questions_owner_bank_idx` | `(owner_id, id DESC) WHERE deleted_at IS NULL AND context_group_id IS NULL` |
      | `question_groups_owner_bank_idx` | `(owner_id, updated_at DESC, id DESC) WHERE owner_section_id IS NULL AND archived_at IS NULL`, mirroring `00049` |
      | `media_assets_owner_idx` | `(owner_id, kind, created_at DESC) WHERE deleted_at IS NULL` |
      | `users_created_by_idx` | `(created_by) WHERE created_by IS NOT NULL` |
      | `assignments_creator_idx` | `(created_by, id DESC)` |
- [ ] `deploy.md` lists the `DROP INDEX CONCURRENTLY IF EXISTS` statements to
      run before redeploying after an interrupted build of any of these or
      `users_role_id_active_idx`, as it already does for `00036` and `00038`.
- [ ] `EXPLAIN (ANALYZE, BUFFERS)` of a teacher's tests list, bank list and
      media library at seeded volume is recorded in the PR. No plan is forced:
      a sequential scan on a small table is correct (`20-data-model.md` §14).
- [ ] up/down/up green.

---

### T-R2.11 — Seeds and a test harness for several staff roles
**Depends on:** T-R2.2, T-R2.5, T-R2.9
**Touches:**
- `seed/06-dev-access.sql` (new), `seed/07-dev-classroom.sql` (new)
- `seed/99-assert.sql`, `seed/README.md`
- `server/tests/harness_test.go`
- the per-module integration harnesses that create users (for example
  `server/internal/modules/tests/application/tests/harness_test.go`)

**Size:** M
**Done when:**
- [ ] Seeds load in lexical order, so each file is numbered at merge, one
      number per file in merge order, as R7 onwards already do (`NN-dev-…`).
      `06` and `07` are the next free numbers today, and R2's seeds merge
      before any later release's. Each file's header names the seeds it
      relies on, if any.
- [ ] `06-dev-access.sql` is idempotent; every user in it has the password
      `quizzivy-dev`, and `role_id` is looked up by `builtin_key`. It seeds:
  - `giaovien@quizzivy.com` (Teacher), with a class, a draft test holding one
    bank question, and one bank group;
  - `giaovien2@quizzivy.com` (Teacher), with its own class and test;
  - `trogiang@quizzivy.com` (Assistant).
- [ ] `07-dev-classroom.sql` seeds thirty students,
      `hocvien.lop01@quizzivy.com` … `hocvien.lop30@quizzivy.com`, with
      `must_change_password = false`, for T-R2.15's live test.
- [ ] `99-assert.sql` asserts:
  - every user has `role_id`;
  - every class has `teacher_id`;
  - every test, question, question group and media asset has `owner_id`;
  - the four built-in roles exist.
- [ ] `thuong@quizzivy.com` (Admin) and `hocvien@quizzivy.com` are unchanged.
      `seed/README.md` lists every login.
- [ ] The Go end-to-end harness gains `createStaff(t, builtinKey)`, and a
      login helper that returns a real token with its epoch. The module
      harnesses that insert users write `role_id`.

---

### T-R2.12a — Scoping: the HTTP edge, tests and question groups
**Depends on:** T-R2.5, T-R2.9, T-R2.11
**Touches:**
- `server/internal/shared/actor/actor.go` (`Actor.Scope`)
- `server/internal/platform/httpapi/httpapi.go`
  (`ActorFromContext` fills the scope; `ScopeFromContext`)
- `server/internal/modules/tests/{application,repositories,http}/**`
- `server/internal/modules/tests/repositories/store.go` (`QuestionLocks`
  gains `OwnerOf`)
- `server/internal/modules/questions/repositories/references.go` (`OwnerOf`)
- `server/internal/core/wiring/tests.go`
- `server/internal/modules/tests/application/tests/scope_test.go` (new)

**Size:** L
**Done when:**
- [ ] `ActorFromContext` builds `Scope{UserID, All}` from the principal, with
      `All` true only for `scope.all`. Queries that serve teacher operations
      gain a `Scope` field. The plumbing lands with its first consumer here,
      because `unparam` forbids unused parameters.
- [ ] Every read and write in the tests module filters `owner_id =
      scope.UserID`, and `All` lifts the filter:
  - lists, facets and search;
  - get, update, delete and publish;
  - versions, set-current and delete-version;
  - preview and restore-as-draft;
  - autosave and the outline.
- [ ] Section-owned groups follow their test; bank groups filter on their own
      `owner_id`.
- [ ] `copyQuestionGroup` needs a readable source and a writable destination.
- [ ] Draft references keep one owner: after `questions.LockForDraftUse`, the
      same transaction reads the question's owner through the new
      `QuestionLocks.OwnerOf`. A question not owned by the test's owner
      answers 404.
- [ ] `LockForDraftUse` itself is unchanged, so its canary still describes the
      path production uses.
- [ ] Duplicate and restore write the source test's owner (T-R2.9).
- [ ] Another teacher's id answers 404, never 403.
- [ ] `scope_test.go`, with teachers A and B:
  - B gets 404 or no rows when listing, faceting, opening, updating,
    publishing, duplicating, previewing, restoring or deleting A's test or
    bank group.
  - B referencing A's question in B's draft gets 404.
  - The Admin opens and edits A's test.
- [ ] Canaries: before and after, and untouched:
  - `tests/application/tests/publish_snapshot_test.go`
  - `questions/application/tests/draftrace_test.go`
    (`TestLockForDraftUseSerialisesAgainstDelete`)
  - `tests/application/tests/publish_lockorder_test.go`
- [ ] No `SELECT *`.

### T-R2.12b — Scoping: questions
**Depends on:** T-R2.12a, T-R2.12c
**Touches:**
- `server/internal/modules/questions/{application,repositories,http}/**`
  (`search.go`, `facets.go`, `store.go`, `owned.go`, `references.go`)
- `server/internal/modules/questions/application/ports/`
  (`MediaKinds.Kind` takes the scope)
- `server/internal/modules/tests/application/ports/group_media.go`
- `server/internal/core/adapters/media.go`
- `server/internal/modules/questions/application/tests/scope_test.go` (new)

**Size:** M
**Done when:**
- [ ] Bank list, search, facets and tagging filter `owner_id`. Get, update,
      delete and duplicate by id need the owner or `All`.
- [ ] `createQuestion` writes the actor as the owner.
- [ ] A question's "used in" list shows only tests the viewer can read.
- [ ] A media binding in a question or a group must name a readable asset,
      as the media module defines it once (T-R2.12c), through the
      `MediaKinds` port. Anything else gives the same validation error as a
      missing asset, so it does not reveal that the asset exists.
- [ ] `scope_test.go` covers B on each of A's operations, B's question binding
      A's audio, and the Admin path.

### T-R2.12c — Scoping: media
**Depends on:** T-R2.12a
**Touches:**
- `server/internal/modules/media/{application,repositories,http}/**`
  (`list.go`, `delete.go`, `store.go`, `references.go`, `group_references.go`)
- `server/internal/modules/media/application/tests/scope_test.go` (new)

**Size:** S
**Done when:**
- [ ] The library lists and facets filter `owner_id`. Delete and the "used in"
      list need the owner or `All`. Upload writes the actor as the owner.
- [ ] The media module defines "readable asset" once, and every binding check
      uses that one definition. An asset is readable when it is the actor's
      own, when the actor holds `scope.all`, or when content the actor can
      read references it; that arm lets R7's copier edit a copy that still
      binds the source owner's asset. R7 adds the share arm and R10 its
      transitive arm to the same definition.
- [ ] Staff mint signed URLs only through a parent they can read: a question,
      a group, a test or a version.
- [ ] `LockForVersionUse` is unchanged. Canary
      `media/application/tests/versionref_test.go`
      (`TestLockForVersionUseSerialisesAgainstDelete`) is green and untouched.
- [ ] `scope_test.go`: B cannot list, delete or see the usage of A's asset.
      The readability definition has one case per arm.

### T-R2.12d — Scoping: classes, students and join
**Depends on:** T-R2.12a
**Touches:**
- `server/internal/modules/classes/{application,repositories,http}/**`
  (`postgres.go`, `lookup.go`, `delete.go`, `joincodes.go`, `enrol.go`)
- `server/internal/modules/identity/{application,repositories,http}/**`
  (`students_students.go`, `students_write.go`, `delete.go`, `students.go`)
- `server/internal/modules/classes/application/tests/scope_test.go` (new)
- `server/internal/modules/identity/application/tests/students_scope_test.go`
  (new)

**Size:** L
**Done when:**
- [ ] Classes filter `teacher_id = scope.UserID`, with `All` lifting the
      filter. This covers:
  - list and facets;
  - get and update;
  - archive and delete;
  - members;
  - rotating and revoking the code.
- [ ] createClass writes the actor as teacher.
- [ ] The "oldest admin" subqueries are gone. The teacher's name comes from
      `classes.teacher_id` in `LookupByCodeHash` (`lookup.go`, the join
      preview) and in `ListMine` (`postgres.go`, the student's classes).
      Today's value is the owner's full name, as before.
- [ ] A student is visible to a teacher when they are student-like and at
      least one of these holds (or with `All`):
  - they are a member of a class the teacher teaches;
  - their `created_by` is the teacher;
  - they are an individual target of an assignment the teacher created.
- [ ] Lists, facets, get and update use that visibility.
- [ ] Inside a visible student, their classes and figures cover only the
      viewer's classes and assignments. One teacher never sees another's class
      names through a shared student.
- [ ] `createStudent`: every `classIds` entry must be a class the actor
      teaches, else 404. It writes `created_by` = the actor.
- [ ] `addClassMember`: the target must be a student visible to the actor,
      else 404.
- [ ] Leak review, recorded in the PR: `/join/preview` still returns exactly
      its three keys (`TestTheSuccessBodyHasExactlyThreeKeys` green), and the
      teacher's name is now the class's own teacher.
- [ ] `scope_test.go` and `students_scope_test.go` cover B on every one of
      A's class and student operations, and B's createStudent into A's class.

### T-R2.12e — Scoping: assignments and attempts
**Depends on:** T-R2.12a, T-R2.12d
**Touches:**
- `server/internal/modules/assignments/{application,repositories,http}/**`
  (`assignments.go`, `write.go`, `delete.go`, `reopen.go`)
- `server/internal/modules/attempts/{application,repositories,http}/**`
  (`admin.go`, `monitor.go`, `review_byquestion.go`, `review_grade.go`,
  `review_review.go`, `integrity_store.go`, `http/admin_attempts.go`)
- `server/internal/modules/assignments/repositories/tests/scope_test.go` (new)
- `server/internal/modules/attempts/application/tests/scope_test.go` (new)

**Size:** L
**Done when:**
- [ ] An assignment is visible when `created_by` is the viewer, or it targets
      a class the viewer teaches, or with `All`.
- [ ] Create and update refuse with 404:
  - a test version of a test the actor cannot read;
  - a target class the actor does not teach;
  - an individual target who is not a visible student.
- [ ] Every teacher attempt operation resolves the attempt, then requires its
      assignment to be visible: monitor, list, review, events, answers by
      question, grade, finish, note, flag, extend, reset and void. The student
      `/app/*` paths are unchanged.
- [ ] Canaries before and after, untouched:
      `attempts/application/tests/events_test.go` and
      `tests/application/tests/publish_snapshot_test.go`.
- [ ] `EXPLAIN (ANALYZE, BUFFERS)` of the monitor, grading and timeline
      queries keeps one query for the parent and one for the children
      (`20-data-model.md` §14), recorded in the PR.
- [ ] `scope_test.go` (both) covers B on every one of A's assignment and
      attempt operations, and B assigning A's version or A's class.

### T-R2.12f — Scoping: dashboard and imports
**Depends on:** T-R2.12a, T-R2.12e
**Touches:**
- `server/internal/modules/dashboard/{repositories/postgres.go,http/handlers.go}`
- `server/internal/modules/imports/{application,repositories,http}/**`
  (`list.go`, `store.go`, `runs.go`, `drafts.go`, `sources.go`, review)
- `server/internal/core/adapters/import_commit.go`
- `server/internal/modules/dashboard/repositories/tests/scope_test.go` (new)
- `server/internal/modules/imports/repositories/tests/scope_test.go` (new)

**Size:** M
**Done when:**
- [ ] Every dashboard aggregate covers the viewer's classes and assignments.
      The student count is the student-like members of those classes. `All`
      reproduces today's numbers exactly, shown by a test against a fixed
      dataset.
- [ ] Imports filter `created_by`, with `All` lifting the filter. This covers:
  - list, get and title search;
  - every mutation;
  - source upload and download;
  - review and commit.
- [ ] Quotas are unchanged (already per creator). The worker's commit writes
      content owned by the import's creator.
- [ ] `scope_test.go` (both) covers B's dashboard excluding A's data, and B
      getting 404 on A's import, on its sources and on its commit.

---

### T-R2.13 — Guards that are not permissions
**Depends on:** T-R2.12d, T-R2.12e, T-R2.12f
**Touches:**
- `server/internal/modules/identity/repositories/{students_students.go,students_write.go,delete.go}`
- `server/internal/modules/identity/application/command/{update_student.go,delete_student.go,reset_student_password.go}`
- `server/internal/modules/identity/http/{students.go,delete.go}`
- `server/internal/modules/identity/domain/errors.go`
- `server/internal/modules/classes/repositories/{postgres.go,delete.go}`
- `server/internal/modules/classes/http/delete.go`
- `server/internal/modules/assignments/repositories/write.go`
- `server/internal/modules/dashboard/repositories/postgres.go`
- `server/internal/core/maintenance/maintenance.go`
- `api/openapi.yaml` (409 descriptions, resetStudentPassword's
  `403 STUDENT_SHARED`), `server/gen/openapi/`,
  `web/src/lib/api/schema.d.ts`
- `server/internal/modules/identity/application/tests/escalation_test.go` (new)
- `server/internal/core/maintenance/tests/maintenance_test.go`

**Size:** M
**Done when:**
- [ ] **Strict student targets.** Every predicate that reads
      `role = 'student'` today uses `role_id IN (SELECT id FROM
      app.student_like_roles)`:
  - `students_students.go` (three places)
  - `students_write.go` (update, reset)
  - `delete.go`
  - `classes/repositories/postgres.go` (AddMember)
  - `assignments/repositories/write.go` (individual targets)
  - `dashboard/repositories/postgres.go` (the student count)
  - `core/maintenance/maintenance.go` (anonymisation)
- [ ] A grep in the PR shows no Go statement that reads or writes the
      `users.role` column.
- [ ] `escalation_test.go` (integration) sets up an Admin with
      `learning.take_tests` granted, who joined a teacher's class by code.
      Then:
  - the teacher's getStudent, updateStudent and resetStudentPassword on that
    Admin answer 404;
  - `addClassMember` refuses them;
  - no assignment can target them individually;
  - `maintenance anonymize-student` refuses them.
- [ ] The same holds for a custom role holding any key besides "Take tests",
      and an empty custom role remains a valid target.
- [ ] **Subset rule.** `deleteUser`, and `updateStudent` with `disabled`,
      check `access.CanActOn(actor, target)` and refuse with 403 `FORBIDDEN`.
  - A Teacher sending `disabled` gets 403, because it lacks
    `people.users.manage`.
  - One handler test covers each operation. The rule's own cases are in
    T-R2.1.
- [ ] **Shared students.** `resetStudentPassword` on a visible student
      requires every active membership of theirs to be in a class the actor
      teaches. A student with no active membership qualifies only when
      `created_by` is the actor. Anything else answers `403 STUDENT_SHARED`,
      with a Vietnamese message as every error body has. An actor holding
      `people.users.manage` is exempt: the Admin resets any student, and
      from R5 does so in the Admin console. R4's bulk reset applies the same
      rule to every row (T-R4.20).
  - The contract documents `403 STUDENT_SHARED` on resetStudentPassword.
  - `escalation_test.go` adds:
    - B resets a student who is in A's class and in B's: 403
      `STUDENT_SHARED`, and the password hash is unchanged;
    - B resets a student only in B's classes: 200;
    - B resets a student in no class whom A created: 404, because B cannot
      see them;
    - the Admin resets the shared student: 200.
- [ ] **Last admin.** The identity repository maps `users_last_admin` to
      `domain.ErrLastAdmin`, and an integration test drives the trigger
      through it. The `409 LAST_ADMIN` response arrives with R5's first
      operation that can reach it.
- [ ] **The Student role keeps "Take tests".** T-R2.1's trigger is tested
      there.
- [ ] **Foreign-key violations answer 409 with a reason.**
  - `classes/repositories/delete.go` and `identity/repositories/delete.go`
    map 23503 and 23001, by constraint name, to `details.referencedBy`. The
    values are `assignments`, `attempts`, `audit`, `members`,
    `owned_content` and `other`.
  - Any 23503 on a foreign key to `app.users` that the map does not name
    answers 409 with `referencedBy: other`, never a 500. Later releases that
    add a restricting foreign key to `app.users` name its constraint in the
    map in their own migration task.
  - Both 409 descriptions in the contract document the field.
  - An integration test builds a disabled student whom a test row names as
    owner, a fixture only the database allows. `deleteUser` answers 409 with
    `referencedBy: owned_content`, never a 500, so any later caller inherits
    the mapping.
  - A unit test of the mapper gives it an unmapped users constraint name and
    gets `other`.
- [ ] The PR lists every other hard delete and why each cannot hit a new R2
      foreign key or already answers 409: `deleteAssignment`,
      `deleteTestVersion`, `deleteQuestionGroup`, `removeClassMember` and
      `revokeJoinCode`.

---

### T-R2.14a — Join codes encrypted: storage and lookup
**Depends on:** T-R2.2
**Touches:**
- `migrations/NNNNN_add_join_code_encryption.sql` (new)
- `server/internal/platform/config/config.go`, and its tests
- `server/internal/modules/classes/domain/joincode.go`
- `server/internal/modules/classes/domain/joincode_keys.go` (new)
- `server/internal/modules/classes/domain/tests/joincode_keys_test.go` (new)
- `server/internal/modules/classes/repositories/{joincodes.go,lookup.go,enrol.go}`
- `server/internal/modules/classes/application/**` (the keys reach the
  commands and the preview)
- `server/internal/core/wiring/classes.go`
- `server/internal/modules/classes/application/tests/joincode_encryption_test.go`
  (new)
- `server/internal/platform/db/tests/expand_r2_test.go`
- `.env.example`, `.github/workflows/ci.yml` (live job env),
  `.github/workflows/deploy.yml` (preflight)
- `docs/setup/dns.md`

**Size:** M
**Done when:**
- [ ] The migration adds to `app.class_join_codes`:
  - `code_ciphertext bytea CHECK (code_ciphertext IS NULL OR
    length(code_ciphertext) = 36)`: a 12-byte nonce, the 8-byte code and a
    16-byte tag.
  - `key_id smallint CHECK (key_id IS NULL OR key_id <> 0)`.
  - `lookup_scheme smallint NOT NULL DEFAULT 1 CHECK (lookup_scheme IN
    (1, 2))`, where 1 is the legacy SHA-256 and 2 is the keyed HMAC.
  - `class_join_codes_scheme_consistent CHECK ((lookup_scheme = 2) =
    (code_ciphertext IS NOT NULL AND key_id IS NOT NULL))`.
- [ ] `code_hash` keeps its 32-byte `UNIQUE` column.
- [ ] Down refuses while an unrevoked, unexpired scheme-2 code exists, because
      the previous binary could not look it up. Otherwise it drops the
      constraint and the columns.
- [ ] Config:
  - `JOIN_CODE_KEY` is required in every environment: standard base64 of
    exactly 32 bytes; the error message says `openssl rand -base64 32`.
  - `JOIN_CODE_KEY_PREVIOUS` is optional, has the same format and must
    differ.
  - Neither value is ever logged.
  - `.env.example` documents both, the CI live job gets a throwaway key,
    `deploy.yml` preflight adds `need JOIN_CODE_KEY`, and `dns.md`'s secrets
    list gains it.
- [ ] Domain: `JoinCodeKeys` derives three keys from each raw key with the
      standard library's `crypto/hkdf` (SHA-256, no salt; no new dependency):
      - info `quizzivy join-code aead v1`: the AES-256 key;
      - info `quizzivy join-code lookup v1`: the HMAC key;
      - info `quizzivy join-code key-id v1`: its first two bytes as a
        non-zero `int16`.
- [ ] Startup refuses equal ids for the current and previous keys.
- [ ] `Seal(classID, codeID, code)`: AES-256-GCM, a random 12-byte nonce,
      AAD = the 16 raw bytes of the class id followed by those of the code id.
      It stores nonce‖ciphertext‖tag.
- [ ] `Open` picks the key by id. An unknown id gives
      `ErrJoinCodeKeyUnavailable`.
- [ ] `LookupHashes(code)` returns the HMAC-SHA256 under the current lookup
      key, under the previous one when set, and the legacy SHA-256.
- [ ] Wiring passes the keys to the classes application. `JoinCodeManager`
      stays stateless for Generate, Format, Normalize and Hint.
- [ ] Rotate:
  - generates the code id in Go (`uuid.NewV7`), because the AAD needs it
    before the insert;
  - writes `code_hash` as the current HMAC, `code_ciphertext`, `key_id`,
    `lookup_scheme = 2` and the hint.
- [ ] Revoke is unchanged.
- [ ] Lookup (`lookup.go`), used by the preview, by joining and by the Google
      join:
  - `WHERE jc.code_hash = ANY($1)`.
  - The row found is compared in constant time with the candidate of its own
    scheme (`JoinCodes.Equal`).
  - A scheme-1 row matches only the SHA-256 candidate, a scheme-2 row only an
    HMAC candidate.
- [ ] No log line and no audit row carries a code or its ciphertext; audit
      rows keep entity ids only.
- [ ] Domain tests:
  - round trip;
  - a swapped class or code id in the AAD fails;
  - a flipped byte fails;
  - the previous key opens an old ciphertext;
  - pinned derivation test vectors.
- [ ] Config tests refuse a missing, short or undecodable key, and equal
      current and previous keys.
- [ ] `joincode_encryption_test.go` (integration):
  - A new code is stored as scheme 2, with a 36-byte ciphertext and a hash
    that is not SHA-256 of the code. A database dump alone no longer yields
    the code by hashing the code space.
  - A legacy scheme-1 row inserted the v0.7.0 way previews, joins and
    Google-joins.
  - A code issued under key K1 still previews and joins after a restart with
    current = K2 and previous = K1.
- [ ] `joincode_preview_test.go`, `joincode_enrol_test.go`,
      `joincode_rotate_test.go` and `joincode_audit_test.go` stay green, with
      only their construction changed.
- [ ] `expand_r2_test.go`: the old binary's insert (no new columns) is a
      consistent scheme-1 row.

### T-R2.14b — Join codes: reading the code back, and rotating the key
**Depends on:** T-R2.14a, T-R2.7, T-R2.12d
**Touches:**
- `api/openapi.yaml` (`getJoinCode`, `JoinCode`, rotateJoinCode's
  description)
- `server/gen/openapi/`, `web/src/lib/api/schema.d.ts`
- `server/internal/modules/classes/application/query/active_code.go`
- `server/internal/modules/classes/http/joincode.go`
- `server/internal/core/router/ratelimits.go`
- `server/internal/core/router/tests/credential_limits_test.go`
- `server/internal/core/maintenance/joincodes.go` (new)
- `server/cmd/maintenance/main.go`
- `server/internal/core/maintenance/tests/joincodes_test.go` (new)
- `docs/setup/operations.md`

**Size:** M
**Done when:**
- [ ] `GET /teacher/classes/{id}/join-code` is `getJoinCode`, with
      `x-permission: teaching.classes.write` (T-R2.16 adds its `x-resource`
      entry, kind `class`):
  - 200 returns `JoinCode {code: string | null (grouped XXXX-XXXX), hint,
    legacy: boolean, expiresAt, maxUses, usesCount}`, with
    `Cache-Control: no-store`.
  - A legacy row returns `code: null, legacy: true`.
  - 404 when the class is not visible to the caller or has no active code.
  - Rate limit: 60/min and 600/h per address. The operation joins
    `theCredentialMinters`, because it reveals a bearer secret.
  - It is not audited: the deck shows the code on every card (D5).
- [ ] rotateJoinCode's description changes: the code is stored encrypted and
      can be read again through getJoinCode. The "shown once, only a SHA-256
      hash" text goes. The response shape is unchanged.
- [ ] `maintenance rekey-join-codes [-apply]`, in the `maintenance <command>
      [flags]` form that R1's per-subcommand parsing gives every command
      (T-R1.12):
  - Refuses to run unless both `JOIN_CODE_KEY` and `JOIN_CODE_KEY_PREVIOUS`
    are set.
  - Every scheme-2 row under the previous key id is opened, then re-sealed
    and re-hashed under the current key. It works in batches of 500, one
    transaction each.
  - It reads `app.class_join_codes` and writes only `code_hash`,
    `code_ciphertext` and `key_id`. R5's `quizzivy_maintenance` grant
    (T-R5.17a) covers that, and its `privileges_test.go` pins it.
  - The dry run reports counts per key id, and the number of scheme-1 rows.
    Those cannot be re-keyed; R4 rotates them.
  - It prints a JSON report like the other commands.
- [ ] getJoinCode tests:
  - as the owner, a scheme-2 code returns the full code;
  - a legacy row returns `null` and `legacy: true`;
  - another teacher gets 404, a student 403.
- [ ] Rekey tests:
  - rows under the old id move;
  - the code still redeems with only the new key configured;
  - the dry run changes nothing.
- [ ] `operations.md` gains a "Join-code key" section:
  - **Generate** it with `openssl rand -base64 32`.
  - **Store** it as a Fly secret, with an offline copy in the owner's
    password manager.
  - **Where the rekey runs:** from the operator's machine, never from a
    scheduled workflow. The operator supplies `JOIN_CODE_KEY` and
    `JOIN_CODE_KEY_PREVIOUS` to `cmd/maintenance` from the offline copy,
    with `MAINTENANCE_DATABASE_URL`. No workflow environment holds the keys.
  - **Rotate:**
    1. set `PREVIOUS` to the old key and the current key to the new one;
    2. deploy;
    3. run the rekey dry run;
    4. run it with `-apply`;
    5. verify zero rows remain under the old id;
    6. unset `PREVIOUS`;
    7. deploy.
  - **Loss:** every scheme-2 code becomes unreadable and unredeemable, so
    rotate every class's code and tell the teachers.
  - The key must never appear in logs or tickets.

---

### T-R2.15 — Join and sign-in limits sized for a classroom
**Depends on:** T-R2.7, T-R2.11
**Touches:**
- `server/internal/platform/ratelimit/{registry.go,bodykey.go}`, and their
  tests
- `server/internal/platform/httpx/ratelimit.go`
- `server/internal/core/router/ratelimits.go`
- `api/openapi.yaml` (`x-rate-limit` blocks)
- `server/gen/openapi/`
- `server/internal/core/router/tests/{join_preview_test.go,router_test.go}`
- `server/internal/core/router/tests/classroom_limits_test.go` (new)
- `web/tests/e2e/join-classroom.live.spec.ts` (new)

**Size:** M
**Done when:**
- [ ] A route can carry several keyed buckets. `ratelimit.Compose(keys...)`
      builds a composite key and yields `""` (skip the bucket) when any part
      is empty.
- [ ] Every limiter keeps its capacity and its eviction, so memory stays
      bounded.
- [ ] The budgets:

      | Operation | Per address | Per address + key | Per key |
      |---|---|---|---|
      | `POST /join/preview` | 60/min, 600/h (was 10, 60) | + code: 10/min | code: 200/h (was 30) |
      | `POST /app/classes/join` | 60/min, 600/h (was 10, 60) | + code: 10/min | code: 200/h (was 30) |
      | `POST /auth/google` | 60/min, 600/h (was 10, 60) | — | join code: 200/h (was 30) |
      | `POST /auth/login` | 60/min, 600/h (was 10, 60) | + email: 10/min | email: 20/h (unchanged) |
      | `POST /auth/refresh` | 120/min, 1200/h (was 30, 200) | — | — |
      | `POST /auth/logout` | 120/min, 1200/h (was 30, 200) | — | — |

  - These numbers let a class of 40 behind one address make three tries at
    each step.
  - The per-address-and-code bucket stops one device hammering a code.
  - The per-email bucket still protects each account.
  - Guessing stays at or under 600 codes an hour per address.
- [ ] The beacon, `POST /app/attempts/{id}/events`, is reviewed and left at
      120/min. It fires only on `pagehide`; the reason is written in the PR.
- [ ] The `x-rate-limit` blocks match the registry.
- [ ] The existing tests are re-pinned to the new boundaries, never deleted:
  - `TestTheEleventhPreviewInAMinuteFromOneAddressIs429` becomes the 61st;
  - a new test: the 11th preview of one code from one address is 429;
  - `TestTheThirtyFirstAttemptOnOneCodeIs429EvenAcrossAddresses` becomes the
    201st;
  - `TestRespellingACodeDoesNotBuyAFreshAllowance` is unchanged.
- [ ] `classroom_limits_test.go`: 40 students, one address, one code. 40
      logins, 120 previews and 40 joins inside one minute see no 429.
- [ ] `join-classroom.live.spec.ts`:
  - The seeded teacher rotates a code.
  - The thirty students from `07-dev-classroom.sql` each sign in with their
    password, preview the code and join, concurrently, from the one test
    client address.
  - Every step succeeds, none gets 429, and the class lists thirty new
    members.
  - Why this belongs in the live suite (AGENTS): it is the deployed limiter
    configuration meeting the real join flow.
- [ ] A leak review of the four public operations is recorded in the PR: the
      bodies are unchanged, only the limits move. T-R2.18 updates spec §6.5's
      numbers.

---

### T-R2.16 — The two-teacher isolation suite
**Depends on:** T-R2.12a–f, T-R2.13, T-R2.14b, T-R2.11
**Touches:**
- `api/openapi.yaml` (`x-resource`, `x-resource-list`)
- `server/gen/openapi/`
- `server/tests/isolation_flow_test.go` (new)
- `server/tests/harness_test.go`

**Size:** L
**Done when:**
- [ ] `x-resource` is a list with one entry per id the operation takes:
      `[{in: path | query | body, name, kind}]`, where `name` is a JSON
      pointer for a body field.
  - The kinds today: test, test-version, section, question, question-group,
    media, import, import-source, class, assignment, attempt and student.
  - A uuid that names no owned resource, such as an option, a gap or an
    integrity session, is declared `kind: none`, so leaving it out is a
    decision that shows in the diff.
  - A query-scoped id is an ordinary entry: `listStudents?classId=` declares
    `{in: query, name: classId, kind: class}`. There is no separate
    `x-resource-param`.
  - Each release adds its kinds to the enum in the PR that adds the
    operation: R4 notification and override; R7 question-set, share and
    attachment; R8 session and session-series; R10 word-list, word, course,
    unit and lesson.
- [ ] Every `/teacher/*` list declares `x-resource-list`.
- [ ] The suite fails on any uuid-typed path, query or body field under a
      scoped tree (`/teacher/*`, `/app/*`, `/me/*`) that names no kind.
- [ ] The suite (tag `e2e`) first seeds, as teacher A, one resource of every
      kind:
  - a test with a draft and a published version;
  - a bank question and a bank group;
  - a media asset;
  - an import;
  - a class with an active code and a student;
  - an assignment with a submitted attempt.
- [ ] Then, as teacher B holding the full Teacher set:
  - Every `x-resource` entry is exercised with a minimal valid body from a
    per-operation table. A's id goes into one parameter at a time, and B's
    own ids fill the others, so a second path id, a query filter and a body
    field are each tested alone.
  - Reads and writes alike answer as a missing id would: 404, or for a body
    reference the same error as an id that does not exist. A's rows are
    byte-for-byte unchanged afterwards.
  - For every list that declares `x-resource-list`, none of A's ids appears
    on any page, and the facets do not count A's rows.
- [ ] B's own writes that reference A's resources answer 404:
  - an assignment for A's version, A's class or A's student;
  - A's question in B's draft;
  - A's media bound in B's question;
  - A's student added to B's class;
  - a copy of A's bank group;
  - a new student created into A's class.
- [ ] B's dashboard figures exclude A's data.
- [ ] As the Admin, a sample of A's resources opens (200).
- [ ] The same substitution runs for two students on `/app/*` (student B
      with student A's assignment and attempt ids) and for two users on
      `/me/*`. The `/me/*` table stays empty until R4 creates the tree.
- [ ] The suite's table names every operation under `/teacher/*`, `/app/*`
      and `/me/*` exactly once, and the test fails naming any operation that
      is missing, or that has a uuid parameter without a kind. A new
      operation therefore cannot skip it: this is the scoping counterpart of
      the open-operations list.
- [ ] It runs inside the Server job's end-to-end step in under a minute. The
      PR template's isolation box is ticked.

---

### T-R2.17 — Web guards by permission, no visual change
**Depends on:** T-R2.6, T-R2.7
**Touches:**
- `web/src/features/auth/permissions.ts` (new)
- `web/src/features/auth/home.ts`
- `web/src/app/guards/RequireRole.tsx` → `RequireWorkspace.tsx`
- `web/src/app/guards/HomeRedirect.tsx`, `web/src/app/router.tsx`
- `web/src/features/join/pages/JoinPage.tsx`
- `web/src/features/auth/components/SettingsPage.tsx`
- `web/src/app/pages/ForbiddenPage.tsx`, and whatever R1 left reading
  `user.role`
- `web/tests/units/auth/guards.test.tsx`
- `web/tests/units/auth/permissions.test.ts` (new)

**Size:** M
**Done when:**
- [ ] `permissions.ts` provides `can(user, key)` and
      `hasWorkspace(user, workspace)`.
  - The hooks subscribe to derived booleans, not to the user object.
  - The `Set` is built once per user object.
- [ ] `homePathFor`: the teacher or admin workspace goes to `/admin`, which is
      still the teacher console until R4; the `app` workspace goes to `/app`.
- [ ] `TeacherWorkspace` (formerly `AdminOnly`) requires the teacher workspace
      and is still mounted at `/admin`.
- [ ] `StudentArea` requires `app`. A staff user without it is redirected home
      as today, and a student on `/admin/*` still gets the 403 page.
- [ ] Every other read of `user.role` is replaced:
  - JoinPage's link target;
  - SettingsPage's label: `app` only shows "Học viên", anything else
    "Giáo viên", which is today's text for both kinds of user.
- [ ] A grep in the PR shows `lib/api/client.typecheck.ts`'s legacy assertion
      as the only read of a user's `role` left in `web/src`.
- [ ] No string is added, and no nav item or palette entry is added or
      removed.
- [ ] `/admin` (dashboard) and `/app` (home) are captured at 1280 and 360
      before and after, and the captures are identical.
- [ ] `guards.test.tsx` covers Admin, Teacher, Assistant and Student fixtures
      carrying the 70 §4.1 sets: where each lands, which trees open, and where
      403 appears. `permissions.test.ts` covers the helpers.
- [ ] Canaries `web/tests/integration/router-chunks.test.ts` and
      `web/tests/units/api/client.refresh.test.ts` are untouched and green.
- [ ] `pnpm lint`, `typecheck`, `format:check`, `test:unit`,
      `test:integration`, `build` and `e2e` are green.

---

### T-R2.18 — Documents for this release
**Depends on:** T-R2.1 … T-R2.17
**Touches:**
- `docs/quizzivy-spec-v0.3.md`
- `AGENTS.md`
- `docs/plan/{20-data-model.md,30-risks.md,50-nfr-registry.md,60-backend-architecture.md,70-redesign-overview.md}`
- `docs/setup/{deploy.md,dns.md}`
- `server/README.md`

**Size:** M
**Done when:**
- [ ] The spec, with its version bumped (§18):
  - §5: roles and the permission catalogue by reference to 70 §4; enforcement
    from `x-permission`; the session epoch; a disabled user refused on the
    next request.
  - §5.4: guards by workspace. §5.5: the docs gate on `system.api_reference`.
  - §6.1 and §6.5: encrypted storage, the keyed lookup, the key and its
    rotation, and T-R2.15's numbers. §6.4: phrased for the class's teacher.
  - §13: `roles`, `permissions`, `role_permissions`, `users.role_id`,
    `session_epoch`, `created_by`, the owner columns and the join-code
    columns; the §13.3 "plaintext never stored" line reversed by D5.
  - §15: the prefix split.
  - The "Availability: R2" markers from T-R0.3 are flipped.
- [ ] `AGENTS.md`, "Authentication and authorization":
  - The "Until R2" prefix bullet is dropped, and the `x-permission` bullet is
    in the present tense.
  - Repositories take `access.Scope`, and another teacher's id answers 404.
  - Student targets go through `app.student_like_roles` and nothing else.
  - The subset rule lives in `access.CanActOn`.
  - A new `/teacher/*`, `/app/*` or `/me/*` operation needs an entry in the
    isolation suite, with a kind for every uuid it takes.
  - The `/admin` alias exists until R3.
- [ ] `AGENTS.md`, other sections:
  - "Redesign in progress": R2 is done.
  - The repository map gains `modules/access` and `shared/access`.
  - The composition-root table gains `wiring/access.go` and
    `Deps.Principals`.
  - "High-risk areas" gains `modules/access`, `users.role_id` and the owner
    columns.
- [ ] `20-data-model.md` records every R2 file in merge order, with the
      deviations at the next free D-numbers:
  - reference data in a migration;
  - `users.role_id` replacing the enum (expand);
  - `owner_id` separate from `created_by`;
  - `classes.teacher_id` backfilled from `classes.created_by` and kept
    separate from it;
  - reversible join-code encryption with a keyed lookup;
  - the `student_like_roles` view;
  - the last-admin trigger, and why its function is `SECURITY DEFINER`.
- [ ] `60-backend-architecture.md`: eleven bounded contexts, `availability`
      (R1) and `access` among them, and the `shared/access` kernel.
- [ ] `50-nfr-registry.md` gains rows for:
  - a permission on every operation, asserted at startup;
  - the cross-owner 404 isolation suite;
  - join codes encrypted at rest with a documented rotation;
  - limits sized for a classroom.
- [ ] `30-risks.md` updates PR-1, PR-2 and PR-5 with the mitigations that
      landed.
- [ ] `70-redesign-overview.md`: §4.2 (the any-of form, the 10-second cache)
      and §4.3 (the subset rule without "Take tests") match what shipped;
      §6's R2 rows marked done.
- [ ] `deploy.md` gains R2's rollout notes (forward only; do not rotate join
      codes during the deploy). `server/README.md` names the new module.

---

### T-R2.19 — Verification
**Depends on:** T-R2.1 … T-R2.18
**Touches:** `docs/setup/rehearsals/r2.sql` (new)
**Size:** M
**Done when:**
- [ ] Every CI job step passes locally, in order, judged by exit code (70 §8):
  - `make lint`, `make gen-check`;
  - `make test-api`, with `docker compose up -d db minio`;
  - goose up, down, up;
  - `pnpm lint`, `typecheck`, `format:check`, `test:unit`,
    `test:integration`, `build`;
  - `pnpm e2e`, `pnpm e2e:content`;
  - `pnpm e2e:live`, because auth and join changed;
  - `node scripts/check-design-deck.mjs`.
- [ ] Per-suite test counts are recorded at the start and at the end of the
      release, and any drop is explained.
- [ ] Security: the isolation suite, the escalation tests and the credential
      limit test are green. Thuong has read `permissions.golden` against
      70 §4.1.
- [ ] The old-tab check:
  1. The v0.7.0 web build (`git checkout v0.7.0 -- web`, in a scratch
     worktree) is served with `vite preview` against the R2 API.
  2. The owner signs in and opens the dashboard, tests, the builder, the
     bank, media, imports, a class and grading.
  3. A student opens and saves an attempt.
  4. Every call succeeds through the alias, and `legacy_admin_path` is
     logged.
- [ ] No screen changed. The old console and the student app at 1280 and 360
      match v0.7.0 captures. The dev server is confirmed fresh; Vite and the
      API are stopped afterwards.
- [ ] `docs/setup/rehearsals/r2.sql` holds the verification queries from the
      release checklist below.

---

## Release checklist

70 §8 applies in full. For R2 specifically:

1. **Prerequisites before merging to `main`.**
   - The `JOIN_CODE_KEY` Fly secret is set; the deploy preflight fails
     otherwise.
   - An offline copy of the key is stored.
   - `JOIN_CODE_KEY_PREVIOUS` is unset.
2. **Neon branch rehearsal (mandatory).** On a branch of production, run
   `goose up` as `quizzivy_migrate`, then `docs/setup/rehearsals/r2.sql`, and
   record the timings. Delete the branch afterwards. The script must show:
   - no user without `role_id`;
   - exactly one Admin (the owner), and the Student count unchanged;
   - no NULL in any owner column, and every owner holding the Admin role;
   - the maximum `updated_at` per table unchanged from before the run;
   - the count of active legacy join codes (`lookup_scheme = 1`, unrevoked,
     unexpired), recorded because R4 rotates them;
   - `pg_index.indisvalid` true for all eight new indexes.
3. **Exam window.** Deploy only when no assignment window has attempts in
   progress; check the monitor or ask Thuong. Scoping changes the attempt
   queries under a running exam.
4. **Deploy the API, then Pages.** While old and new machines overlap, a join
   code rotated by the new binary cannot be previewed by an old machine. Do
   not rotate codes during the deploy.
5. **Smoke on production:**
   - `/livez` and `/healthz` answer, and the worker process group is running.
   - The owner signs in and lands in the old console at `/admin`.
   - Dashboard, the tests list, the builder, the bank, the media library,
     `GET /teacher/imports/capabilities`, an assignment monitor and grading
     all work.
   - A student signs in, and an assignment intro opens.
   - `/join/preview` with an existing legacy code answers.
   - `GET /auth/me` carries `permissions` and `workspaces`.
   - A tab left on v0.7.0 still works, and `legacy_admin_path` appears in the
     logs.
6. **Watch for 24 hours:**
   - 401 and 403 rates: a mis-mapped permission shows as `FORBIDDEN` on a
     staff operation;
   - `legacy_admin_path` counts, which should fall;
   - Neon compute hours against the week before (the principal cache;
     PR-7).
7. **Release notes** (`docs(release): v0.8.0`):
   - Nothing looks different.
   - Teaching API paths moved to `/teacher/*`; the old paths are served until
     the next release.
   - The permission and ownership model is in place; there is no second
     teacher yet.
   - New join codes are stored encrypted, and existing codes keep working
     until R4 rotates them.
   - Join and sign-in limits now fit a classroom behind one address.
   - Forward-only: v0.7.0 cannot read codes that v0.8.0 issued, so roll
     forward.
8. **Tag and close.** Tag `v0.8.0`, back-merge `main` → `develop` by PR (the
   #151 pattern), and close the epic.
9. **Precondition for R3's contract steps.** They merge only after v0.8.0 has
   been the only binary in production for at least a week, with
   `legacy_admin_path` hits near zero. R1's new-version prompt covers the tabs
   that remain.

## Open items

- **Classroom size.** The per-code budget of 200 an hour assumes classes of
  up to 40 and three tries per step (T-R2.15). Default: build as proposed. If
  Thuong runs larger sessions, such as a whole year group in a hall, raise the
  per-code bucket only.
- **Enforcing `mustChangePassword` on the server.** Today only the SPA
  redirects a user with a temporary password; the API serves them in full.
  `RequirePermission` could refuse every operation but `self` until the
  password changes. That is not in R2, because it would change behaviour for
  sessions already in flight. Default: not enforced in R2. R5 takes the
  decision with its set-password link (D15, DG-05) and proposes
  `403 PASSWORD_CHANGE_REQUIRED` (O-R5.4).
- **The two amendments to 70 §4:** the any-of form of `x-permission` (§4.2),
  and the subset rule ignoring "Take tests" (§4.3, D13). 70 carries both.
  Default: build as written here. Thuong confirms the D13 reading.
- **The Admin's lists in the teacher workspace.** They show everything in R2
  (`scope.all`), which is no change while the owner owns everything. R4
  decides the default (DG-53). The proposal: content lists show the caller's
  own rows, admins included; Students and Classes keep `scope.all` until
  R5.
- **Resetting a shared student's password.** Default: a teacher's reset is
  refused with `403 STUDENT_SHARED` unless every active membership of the
  student is in the actor's classes (T-R2.13). A holder of
  `people.users.manage` is exempt. It is the safer reading, because the
  alternative lets one teacher take over a student another teacher shares,
  and from R7 read their direct messages (D17). Thuong confirms.
