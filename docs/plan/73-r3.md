# R3 — Student console (v0.9.0)

The student half of the redesign. Every `/app` screen and the take-test engine are rebuilt to
`docs/design/deck/Quizzivy Student.dc.html`. The release also carries the contract half of R2's
expand steps: the legacy role enum goes, the ownership constraints are validated, and the
`/admin` path alias is removed. No teacher or admin screen changes. Where the deck is silent,
`docs/design/gaps.md` says what to build: DG-80 (engine frames), DG-81 (URLs) and DG-82 (badge,
bell, notification switches).

Two constraints shape the file.

The first is risk. The engine is the highest-risk code in the product (70 §9 PR-3;
`30-risks.md` R-01, R-03, R-05, R-06), so it is rebuilt in four sub-PRs. Each sub-PR runs the
five canaries on its branch point and again on its last commit, together with
`pnpm test:unit tests/units/take-test tests/units/integrity tests/units/media`. The five
canaries are:

- `server/internal/modules/tests/application/tests/publish_snapshot_test.go`
- `web/tests/units/media/audio-player.test.tsx` (AGENTS' `AudioPlayer.test.tsx`)
- `server/internal/modules/attempts/application/tests/events_test.go`
- `web/tests/units/api/client.refresh.test.ts`
- `web/tests/integration/router-chunks.test.ts`

The second is order. R3 and R4 are independent (70 §2 rule 4). If R4 ships first, T-R3.1 to
T-R3.3 move to R4 unchanged. In that case the student bell and notification settings, which
this file gates on R4's module, are built here instead.

Branch `work/redesign-r3` is cut from `develop`. Feature branches are `feature/t-r3-<nn>-<slug>`.

**Deliverable:** the `/app` tree (Home, Classes with the Join dialog, Test intro, Result,
Settings) under a rebuilt `StudentLayout`. The engine is rebuilt in `FocusLayout` with split
passage and question panes, a footer number strip, a phone question sheet, flags, the focus
dialog, the copy-and-paste toast and the red timer. It keeps autosave and local drafts,
integrity capture, same-tick audio, section grouping and single-flight refresh. Learn, Grades,
Messages, This week, Practice and the bell stay absent until their modules ship. The R2
contract steps are done.

**Exit criteria:**

- Every `/app` route matches the deck at 360, 768, 1024, 1280 and 1440, in light and dark.
- The five canaries pass untouched before and after every engine PR.
- Live E2E 2 and 5–9 pass, with E2E 9's key list unchanged.
- `users.role` and `app.user_role` are gone, and no `NOT VALID` constraint is left in `app`
  outside the pinned list.
- The release ships to production as `v0.9.0`.

**Decisions taken in this file, worth a second look.**

- **The shell branches at 768px through a media query; pages use container queries.** The
  deck measures its root element with a ResizeObserver. The student shell *is* the viewport,
  so `useMediaQuery("(min-width: 768px)")` gives the same number and keeps
  `tests/support/viewport.ts` working. Thresholds inside a page (Home's two columns at 820, the
  part tiles) are CSS container queries on the main column. They follow the column rather than
  the window, and need no JavaScript. The delivery draft's `useContainerWidth` hook is not
  added.
- **The deck's Submit dialog replaces the full-page review screen.** The deck draws no review
  page. The dialog carries the same facts: answered, flagged, time left, and a "Go to" chip per
  unanswered question. The submit path is unchanged: it flushes, then submits, and stays
  idempotent against a racing auto-submit. Only the surface moves.
- **On phones, the save state appears only while something is unsaved.** The deck shows "All
  answers saved" from 768 up and nothing on phones. A phone whose saves are failing must say
  so. A one-line strip under the header appears for Saving…, offline and failed, and goes away
  once everything is saved. It is listed for DG-80 sign-off.
- **An unknown question type renders a "reload" block, never a choice list.** Today the
  engine's `default` branch renders any unknown type as options and writes a `choice` answer.
  R6 adds three types, and a tab that loaded this engine earlier would mis-answer them. The
  block names the question, writes nothing, and offers to reload. R1's update prompt stays out
  of `FocusLayout`.
- **Keys are the deck's a–e plus the existing F for flag.** The deck binds a–e. The engine
  binds F to flag today, so a key for a sixth option would collide with it. Options after E
  are reached with Tab. Arrow keys and Esc work as the deck draws them.
- **"Larger text in tests" is stored locally until R4's `users.preferences` exists.** It lives
  in `localStorage['quizzivy.testText']`, read inside try/catch. R4 moves it to
  `preferences.largerTestText`, migrating the local value once (74-r4.md); after that the
  server value wins. This follows the theme rule in 70 §6.
- **The resume card's answered count applies the engine's rule in SQL.** "3 of 8 answered"
  must agree with the navigator's squares, which follow `take-test/answered.ts`: a fill-in
  counts only when every blank is filled. The student assignment list counts saved answers by
  the same rule, pinned by an integration test that runs the unit test's cases. An answer
  that exists only in the local draft is not counted.
- **Part tiles are summed on the client from sections.** The result gains `sections` and
  `ResultQuestion.sectionId`, both safe for students, and the page sums `earned` per part. A
  server-side `parts[]` would repeat the review-policy gate in a second place.
- **R3 has no attempt-level comment callout.** The deck's "Comment" callout has no model.
  `attempts.teacher_note` is the teacher's private note and must never reach a student.
  Per-answer grader comments stay on their review items. R6 handles the one case the deck
  draws, a single hand-graded essay.
- **The bottom tab bar shows only destinations that exist.** In v0.9.0 those are Home, Classes
  and Me. The deck's five tabs return one by one as R9 (Grades) and R10 (Learn) ship (70 §2
  rule 5). Nav items, the Messages button, the bell and the page controls read one availability
  map.

---

### T-R3.1 — Retire the legacy role (contract step)
**Depends on:** v0.8.0 (R2) the only production binary for at least seven days (72-r2.md
release checklist item 9)
**Touches:** `migrations/NNNNN_drop_users_legacy_role.sql` (new), `api/openapi.yaml` (`User.role`, `CurrentUser.role`, `Role`), `server/gen/openapi/`, `web/src/lib/api/schema.d.ts`, `server/internal/platform/httpx/auth.go`, `server/internal/modules/identity/application/token/token.go`, `server/internal/modules/identity/repositories/store.go`, `seed/01-dev.sql`, `seed/03-dev-students.sql`, `server/internal/core/tests/legacy_role_test.go` (new), `server/internal/platform/db/tests/contract_r3_test.go` (new), `docs/plan/20-data-model.md`
**Size:** M
**Done when:**
- [ ] Before the first commit, a grep of the `v0.8.0` tag finds no query, seed or test that
      names `users.role`, `u.role` or `app.user_role` outside `migrations/`. The output goes in
      the PR. v0.8.0 is the binary still running during the deploy window, so this check is
      what makes dropping the column safe.
- [ ] The migration runs in one transaction, using the names 72-r2.md created:
      - `ALTER TABLE app.users VALIDATE CONSTRAINT users_role_id_not_null`
      - drop the trigger `users_sync_legacy_role` and its function
      - drop `users_role_active_idx`
      - drop `users.role`
      - `DROP TYPE app.user_role`
- [ ] Down restores the legacy role and passes goose up/down/up in CI:
      - it recreates `app.user_role` and grants `USAGE` to `quizzivy_app`
      - it adds `role` as nullable and backfills it through the view R2 made the single
        strict student predicate, which survives R3:
        `CASE WHEN role_id IN (SELECT id FROM app.student_like_roles) THEN 'student' ELSE
        'admin' END`. A grants-only rule would classify the Admin, which stores at most its
        "Take tests" cell, as a student.
      - it sets `NOT NULL DEFAULT 'student'`
      - it recreates the index and the sync trigger
- [ ] A Down test in `contract_r3_test.go` (integration) seeds an Admin whose role has
      `learning.take_tests` granted, a Teacher and a Student, runs the Down, and asserts
      `role` is `'admin'`, `'admin'` and `'student'`.
- [ ] `legacy_role_test.go` walks `server/`, `seed/` and `docker/`, skipping `migrations/` and
      `server/gen/`. It fails on `app.user_role`, `users.role`, `role = 'admin'` or
      `role = 'student'`.
- [ ] Access tokens no longer carry the legacy `role` claim. The parser ignores the claim if
      present, so a v0.8.0 token presented during the deploy window still authenticates. A
      unit test uses a fixture token.
- [ ] `User.role`/`CurrentUser.role` and the `Role` schema, which R2 T-R2.6 kept, leave
      `api/openapi.yaml`:
      - `make gen` is its own commit
      - any remaining web call site is fixed in the same PR, and `pnpm typecheck` passes
      - `GET /auth/me` has a response test
- [ ] Integration test: inserting a user without `role_id` fails with the not-null violation.
- [ ] The users section of `20-data-model.md` and its §12 deviation entry record that the enum
      is gone, and spec §13's users table matches.

---

### T-R3.2 — Validate ownership and drop the fill triggers (contract step)
**Depends on:** v0.8.0 (R2) the only production binary for at least seven days (72-r2.md
release checklist item 9)
**Touches:** `migrations/NNNNN_validate_ownership.sql` (new), `server/internal/platform/db/tests/constraints_test.go`, `server/internal/platform/db/tests/contract_r3_test.go`, `docs/plan/20-data-model.md`
**Size:** S
**Done when:**
- [ ] The migration covers every owner column that R2 constrained `NOT NULL … NOT VALID`
      (72-r2.md T-R2.9). It validates, by name, `classes_teacher_id_not_null`,
      `tests_owner_id_not_null`, `questions_owner_id_not_null`,
      `question_groups_owner_id_not_null` and `media_assets_owner_id_not_null`. For each one:
      - a `DO` block raises, naming the table, if any row is still NULL
      - then `VALIDATE CONSTRAINT` runs, under `SHARE UPDATE EXCLUSIVE`, so reads and writes
        continue
- [ ] The fill triggers and their functions are dropped, using the names T-R2.9 created and no
      `IF EXISTS`, so a wrong name fails the migration instead of leaving a trigger behind:
      - the BEFORE INSERT triggers on `tests`, `questions` and `question_groups`, then
        `app.fill_owner_from_created_by()`
      - the BEFORE INSERT trigger on `media_assets`, then `app.fill_owner_from_uploaded_by()`
      - the trigger `classes_fill_teacher` on `classes`, then its function
- [ ] The previous binary (v0.8.0) sets every owner itself. `contract_r3_test.go` asserts that
      neither fill function, the `classes_fill_teacher` trigger nor its function exists.
- [ ] Down recreates the functions and triggers. The constraints stay validated, because Down
      never weakens a valid NOT NULL.
- [ ] `constraints_test.go` pins the not-validated constraints in `app` to an explicit list,
      empty after this release:
      - a later expand step adds its constraint to the list, and its contract step removes it
      - a forgotten `NOT VALID` fails CI
      - an insert that omits an owner fails
- [ ] Rehearsed on a Neon branch of production. The validation time per table goes in the PR.
- [ ] `20-data-model.md` records the contract half of each ownership deviation.

---

### T-R3.3 — Remove the `/admin` legacy alias (contract step)
**Depends on:** v0.8.0 (R2) the only production binary for at least seven days (72-r2.md
release checklist item 9)
**Touches:** `server/internal/core/router/legacy_admin.go` (removed; the outer alias handler 72-r2.md T-R2.8 added), `server/internal/core/router/legacy_admin_paths.go` (removed), `server/internal/core/router/tests/legacy_admin_test.go` (removed), `server/internal/core/router/router.go` (the alias wiring), `server/internal/core/router/ratelimits.go`, `server/internal/core/router/tests/router_test.go`, `AGENTS.md`
**Size:** S
**Done when:**
- [ ] The PR records the number of `legacy_admin_path` log lines in production over the seven
      days before merge, and the paths seen. Traffic that is not a stale browser tab, such as a
      script or a bookmarked API path, is named and dealt with before the alias goes.
- [ ] The handler (`legacy_admin.go`), its pinned path list (`legacy_admin_paths.go`) and its
      test (`legacy_admin_test.go`) are removed together, and `router.go` no longer wraps the
      mux with it. The PR says why the test goes: the behaviour it pinned is removed on
      schedule, and the test is passing, not failing.
- [ ] A former teaching path under `/admin/` now gets the same 404 as any path the contract
      does not declare. `router_test.go` covers it.
- [ ] `RateLimits()` holds no key for an aliased path. The test that every credential-minting
      operation has a limit still passes.
- [ ] The R2 bullet in AGENTS.md's "Redesign in progress" no longer mentions an alias.

---

### T-R3.4 — Student contract: resume progress and result parts
**Depends on:** —
**Touches:** `api/openapi.yaml` (`StudentAssignmentCard`, `ResultQuestion`, `getAttemptResult`), `server/gen/openapi/`, `web/src/lib/api/schema.d.ts`, `server/internal/modules/assignments/{domain/student_view.go,repositories/student.go,http/student.go}`, `server/internal/modules/assignments/repositories/tests/student_test.go`, `server/internal/modules/attempts/{repositories/result.go,http/result.go}`, `server/internal/modules/attempts/application/tests/result_test.go`, `web/tests/support/{fixtures,handlers}.ts`, `docs/quizzivy-spec-v0.3.md` §15
**Size:** M
**Done when:**
- [ ] `StudentAssignmentCard.liveAnsweredCount: integer | null` counts the live attempt's
      saved answers that say something. It is null exactly when `hasLiveAttempt` is false.
- [ ] The count applies the rule in `web/src/features/take-test/answered.ts`, in SQL:
      - choice: `optionIds` is non-empty
      - true/false: `value` is a boolean
      - text: the value is non-blank
      - fill-in: every blank of the frozen question is non-blank

      `student_test.go` runs the same cases as `web/tests/units/take-test/answered.test.ts`.
- [ ] The result gains `sections: StudentSection[]` and `ResultQuestion.sectionId`. Both appear
      under every review policy: a part's title and membership reveal nothing the paper did
      not already show.
- [ ] Neither `/app/*` operation changes its `x-permission`, and no operation is added.
- [ ] The PR's leak review says neither field is key-shaped.
      `web/tests/units/contract/openapi-contract.test.ts` and
      `server/internal/modules/attempts/application/tests/payload_test.go` pass unchanged.
- [ ] `make gen` is its own commit. MSW fixtures validate with ajv, and `pnpm typecheck`
      passes.
- [ ] Spec §15 documents both fields.

---

### T-R3.5 — Student shell
**Depends on:** R1 (tokens, primitives, theme, splash hand-off)
**Touches:** `web/src/layouts/StudentLayout.tsx`, `web/src/layouts/detailShell.ts`, `web/src/layouts/slots.ts`, `web/src/app/router.tsx`, `web/src/app/modules.ts` (new unless an earlier release added it), `web/src/features/auth/AccountMenu.tsx`, `web/src/features/auth/SignOutButton.tsx`, `web/src/components/shared/Brand.tsx`, `web/src/lib/i18n/locales/{vi,en}.json`, `web/tests/units/student/shell.test.tsx` (new, replaces `desktop.test.tsx`), `web/tests/e2e/student-layout.spec.ts`
**Size:** L
**Done when:**
- [ ] There is one `StudentLayout` outlet. `useMediaQuery("(min-width: 768px)")` selects only
      the chrome, and a form's state survives crossing 768 (test).
- [ ] The top bar is 60px high. It holds:
      - the mark (colour, or on-dark by theme) and the wordmark, linking to `/app`
      - from 768, the nav: Home (house) and Classes (users)
      - Learn (R10) and Grades (R9), only once `modules.ts` says their module has shipped
- [ ] Home carries the DG-82 badge: the number of assignments not yet completed that are open,
      or will open, and close within seven days.
- [ ] Messages (R7) and the bell (R4) render only when their modules are on. Whichever of R3
      and R4 ships second wires the student bell per DG-82: a dot only when something is
      unread, and "Mark all read".
- [ ] The avatar button shows initials from given and family name (DG-22). It opens the 240px
      account menu:
      - name and email
      - Settings
      - Dark mode or Light mode, using R1's theme
      - Sign out, in danger ink under a divider

      The menu follows the WAI-ARIA pattern: roving focus, and Esc returns focus to the button
      (DG-36).
- [ ] Below 768 the top nav is hidden and a bottom tab bar shows:
      - the root destinations that exist: Home, Classes and Me (→ `/app/settings`) in v0.9.0;
        Learn and Grades once shipped
      - 52px tabs with the icon in a 48×28 pill; the active pill is `--accent-soft`
      - the bar is hidden on detail routes, and toasts sit above it
- [ ] Detail routes declare `handle.detail = { titleKey, back }`, all going back to `/app`:
      `assignments/:id` (Test), `attempts/:attemptId/result` (Result) and `settings/:section?`
      (Settings). Below 768, the logo becomes a 36px back button with the title. From 768 the
      nav stays and the page draws its own back link.
- [ ] Main area:
      - padding 32/24/48, or 18/16/28 below 768
      - h1 26px, or 22px below 768
      - scroll resets on navigation
      - each page sets its own maximum width (T-R3.6 to T-R3.11)
- [ ] No `PageAside`, `PageAsideSlot` or `SideColumn` renders under `StudentLayout`.
- [ ] No "Coming next" route. The deck's unreachable placeholder is prototype chrome; an
      unshipped module is hidden instead (70 §2 rule 5).
- [ ] Nothing polls. The badge and the lists refresh on focus and after mutations (70 §9 PR-7).
- [ ] The student skeleton is registered with R1's splash hand-off.
- [ ] Dark mode opens on the student side. `StudentLayout` no longer calls R1's
      `useForcedLightTheme()`, and the mark uses `Brand theme="auto"`. Light and dark both
      render from tokens.
- [ ] Unit tests cover:
      - the nav items match the available modules (flipping a flag in the test adds the item)
      - the tab bar at `viewport("phone")`
      - the detail header on a phone
      - the account menu's keyboard behaviour and Esc
      - the badge count from a fixture
- [ ] Compared with the deck's Home frame at the five widths, light and dark. The PR lists the
      new vi and en keys.

---

### T-R3.6 — Home
**Depends on:** T-R3.4, T-R3.5
**Touches:** `web/src/features/assignments/pages/StudentHomePage.tsx`, `web/src/features/assignments/components/StudentPanel.tsx` (removed), `web/src/features/assignments/components/{ResumeCard,ComingUp,RecentResults}.tsx` (new), `web/src/features/assignments/{status,studentTime}.ts`, locales, `web/tests/units/student/home.test.tsx`
**Size:** M
**Done when:**
- [ ] The column is at most 960 wide. The greeting follows the time of day and uses the given
      name, the last word of the full name. The line under it is derived from the lists, never
      fixed copy: either the live attempt's closing time or "Nothing due today. Next up: …".
- [ ] A resume card shows the live attempt that closes soonest:
      - an info pill, "In progress · n min left"
      - the title
      - class · "a of q answered" (`liveAnsweredCount`) · closing time
      - a 6px `--accent-c` bar at a/q
      - "Continue test", 46px high and full width below 768

      Any other live attempt stays in Coming up with an "In progress" pill.
- [ ] Coming up shows a count in its header. Each row has:
      - a 44px date tile
      - the title
      - class · minutes · questions
      - a pill: Due tomorrow (warning, only when closing within 24 hours), Open now (success),
        or Opens {weekday} (muted)

      A row opens the intro.
- [ ] Recent results show at most three entries from `completed`:
      - "8 / 10" only when `score` is present
      - "Being graded" (warning, hourglass) while `pendingManual > 0`
      - otherwise "Submitted"
      - the newest reads "Just now" for a minute after submitting
- [ ] The Practice card is absent until R10. Until then recent results take the full width. The
      1.4fr / 1fr split at a container width of 820 or more applies only once Practice exists.
- [ ] States (DG-83):
      - a loading skeleton
      - no classes: one sentence and "Join a class", which opens T-R3.7's dialog
      - nothing assigned
      - an error with retry
- [ ] Unit tests cover every state and each pill boundary. Compared with the deck at the five
      widths, light and dark.

---

### T-R3.7 — Classes and the Join dialog
**Depends on:** T-R3.5
**Touches:** `web/src/features/classes/pages/StudentClassesPage.tsx`, `web/src/features/join/components/JoinDialog.tsx` (new), `web/src/features/join/components/{JoinCodeField,ClassPreviewCard}.tsx` (from R1), `web/src/features/join/{code,api}.ts`, `web/src/features/classes/api.ts`, locales, `web/tests/units/student/classes.test.tsx`, `web/tests/units/join/join-dialog.test.tsx` (new), `web/tests/e2e/join.spec.ts`
**Size:** M
**Done when:**
- [ ] The column is at most 960 wide, with the h1, the line under it and a primary "Join a
      class" button. "This week" is absent until R8.
- [ ] Cards sit in an auto-fill grid with a 280px minimum. Each card shows:
      - the class name
      - the timetable line, absent until R8
      - the teacher row: initials, `teacherName` (the class's `teacher_id` since R2) and the
        caption "Teacher"; the message button is absent until R7
      - a "Next" footer: the soonest open or upcoming assignment with this `classId`, or
        "Nothing assigned yet"

      Cards are not links, and there is no way to leave a class (deck).
- [ ] The dialog is 440px wide. It reuses R1's `JoinCodeField` and `ClassPreviewCard`
      (71-r1.md), sized to the dialog's 52px input, and keeps their rules:
      - uppercase, with the dash after four characters, capped at eight
      - the invalid-format state for characters outside the §6 alphabet
      - the placeholder `K7QM-2PXA` (DG-02)
- [ ] The dialog sends at most one `POST /join/preview` per distinct complete code, after
      R1's debounce, and none per keystroke; a test counts the requests. It must not spend the
      per-IP budget R2 sized for a class joining together behind one school address.
- [ ] Preview outcomes:
      - found: the preview card, with the class name and teacher; schedule and room only from
        R8
      - already a member (the preview's `classId` is in `/app/classes`): "You are already in
        this class.", and Join is disabled
      - any failure `code`: R1's single line, "No class uses this code. Check it with your
        teacher.", and nothing about any class reaches the DOM
      - 429: the server's message, with the `Retry-After` wait (DG-40)
- [ ] Join calls `POST /app/classes/join` and shows the toast "You joined {name}". The classes
      and assignments queries refetch, and focus returns to the trigger. Esc closes the dialog.
- [ ] States: loading, no classes (the dialog is the call to action), and an error with retry.
      Compared with the deck.

---

### T-R3.8 — Test intro
**Depends on:** T-R3.5
**Touches:** `web/src/features/assignments/pages/AssignmentIntroPage.tsx`, `web/src/features/assignments/studentRules.ts`, `web/src/features/assignments/components/StudentRulesPreview.tsx`, locales, `web/tests/units/student/intro.test.tsx`, `web/tests/units/assignments/student-rules.test.ts`
**Size:** M
**Done when:**
- [ ] The column is at most 720 wide: back link "Home" from 768, then the class label and the
      h1.
- [ ] A facts strip has three cells: time limit, questions, and attempts ("used of max").
- [ ] "Before you start" is generated only from the stored policy and dates. vi is written
      first; en uses the deck's wording. The rules cover:
      - availability: available until a date, or the opening range
      - the timer does not pause
      - copy and paste are off, when `blockCopyPaste` is set
      - leaving, per `maxFocusLoss` and `onLimitExceeded`: nothing for 0, "not allowed" for −1,
        "n times, after that your teacher is told" for `flag` (D8), and the `warn` and
        `auto_submit` variants
      - fullscreen, when required
      - audio plays, from `audioMaxPlays` and shared audio
      - the score line, only when `review.showScore`, with R4's release timing when present
- [ ] `student-rules.test.ts` runs every combination of integrity and review policy and asserts
      that each sentence matches the policy it came from. The teacher's `StudentRulesPreview`
      uses the same generator.
- [ ] The teacher's note renders only when the student assignment detail carries
      `studentNote`, which R4 adds with `assignments.student_note` (74-r4.md; the gap entry
      "Assignment note to students"). Until then nothing renders in its place.
- [ ] A sticky action sits over a fade from transparent to `var(--bg)`:
      - "Start test", with "Your answers save automatically."
      - "Continue test" for a live attempt
      - "Opens Monday", inert, for a scheduled assignment
      - blocked states (closed, no attempts left) stated in words
- [ ] The "Start now?" dialog offers Not yet and Start. Start enters fullscreen in the same
      gesture when the policy requires it (test).
- [ ] Compared with the deck, including the upcoming variant.

---

### T-R3.9a — Engine: focus shell, header, timer and save state
**Depends on:** R1
**Touches:** `web/src/layouts/FocusLayout.tsx`, `web/src/features/take-test/pages/TakeTestPage.tsx`, `web/src/features/take-test/components/{EngineHeader,Clock,SaveState}.tsx`, `web/src/features/take-test/store.ts` (read only), locales, `web/tests/units/take-test/{timer,breakpoint,load-failure,resume,deadline}.test.ts(x)`, `web/tests/units/take-test/draft-compat.test.ts` (new)
**Size:** M
**Done when:**
- [ ] The five canaries and the take-test, integrity and media unit suites pass on the branch
      point and on the last commit. Both runs go in the PR.
- [ ] The header is 60px high:
      - ✕ (36px, "Leave test")
      - from 768, the title with the save state under it: "All answers saved", "Saving…",
        offline or failed, read from the store rather than timed
      - the timer pill, centred
      - Submit (primary)
- [ ] The timer runs from the server's `deadlineAt` and `serverTime`, as today, and
      `deadline.test.ts` is unchanged. Under 5:00 it takes the danger tones (D8). A polite live
      region announces 5:00 and 1:00.
- [ ] Below 768, a one-line save strip appears only while something is unsaved (decision
      above; DG-80).
- [ ] The Leave dialog reads "Leave the test?" and offers Stay and Leave. Pending answers flush
      before navigating to `/app`.
- [ ] R1's app-state overlays keep working over the rebuilt engine (tests):
      - maintenance and session-expired render above the engine, which stays mounted and writes
        its local draft on every change
      - the store adopts the server's extended deadline, as R1 made it
      - `useIntegrityMonitor` records no focus change while an overlay shows
- [ ] `FocusLayout` no longer calls `useForcedLightTheme()`: the engine follows the theme
      preference.
- [ ] The take-test route keeps `handle.focus`, so R1's update prompt never renders inside
      `FocusLayout` (test).
- [ ] `draft-compat.test.ts`:
      - a `quizzivy.answer-draft.<attemptId>` entry in the v0.8.0 format restores its answers
      - `quizzivy.flags.<attemptId>` restores its flags
      - neither key nor schema changes
- [ ] Unit tests run at 1280 and at `viewport("phone")`.

---

### T-R3.9b — Engine: passage and question panes
**Depends on:** T-R3.9a
**Touches:** `web/src/features/take-test/components/{QuestionCard,QuestionBody,GroupContext,QuestionAudio,SectionInstructions,Note}.tsx`, `web/src/features/take-test/components/blankInputs.ts`, `web/src/features/take-test/components/UnknownType.tsx` (new), `web/src/features/take-test/{sections,worth}.ts`, `web/src/features/media/components/AudioPlayer.tsx` (styling only), `web/src/features/tests/components/StudentPreview.tsx`, locales, `web/tests/units/take-test/{renderers,group-context}.test.tsx`
**Size:** L
**Done when:**
- [ ] From 768, a question whose group has stimuli splits into two panes:
      - passage: at most 640 wide, with the part eyebrow, the title and paragraphs at 16px,
        line-height 1.75, not selectable
      - question: at most 600 wide, on the `--sidebar` background

      A question with no passage uses the question pane alone, centred.
- [ ] Below 768, a "Passage | Question n" switcher sits under the header when a passage
      exists. The question shows first.
- [ ] The question header reads "Question 4 of 8 · Choose one", with the label for each type.
      Beside it is the flag toggle, "Flag for review" or "Flagged", in warning tones.
- [ ] The prompt is 17px. Options are at least 52px high with letter markers (round for single
      choice, square for multiple), and a chosen option gets a primary border and a check. Text
      inputs are 52px high, and paste is blocked when the policy blocks it.
- [ ] Every existing type and context renders in deck primitives:
      - single choice, multiple choice and true/false; true/false keeps writing the `choice`
        answer it writes today (see 76-r6.md, open items)
      - fill-in, with rich blanks and the case-rule line
      - short answer
      - images, question audio and group recordings
      - cloze groups
      - section instructions on the first question of a section

      In dark mode, images and rich tables keep a light paper surface (DG-35). The PR lists the
      frames the deck does not draw, for DG-80 sign-off.
- [ ] An unknown `type` renders `UnknownType` and writes no answer. A unit test uses a
      fabricated type.
- [ ] "Larger text in tests" sets passage, prompt and options to 18, 19 and 17px.
- [ ] `audio-player.test.tsx` passes untouched: `.play()` stays in the click's synchronous tick.
- [ ] Order is unchanged: `sections.test.ts` passes, and options keep the server's order.
- [ ] The teacher's draft preview renders the same panes.

---

### T-R3.9c — Engine: number strip, question sheet, flags and keys
**Depends on:** T-R3.9b
**Touches:** `web/src/features/take-test/components/Navigator.tsx` (rewritten), `web/src/features/take-test/pages/TakeTestPage.tsx`, `web/src/hooks/useColumnWidth.ts`, locales, `web/tests/units/take-test/{navigator,keyboard,flags}.test.ts(x)`, `web/tests/units/shared/side-column.test.tsx`
**Size:** M
**Done when:**
- [ ] From 768, the footer holds:
      - Previous
      - a wrapping strip of 34px squares: answered is filled primary, the current question has
        an `--accent-c` border, and a flagged one has an amber corner dot
      - Next, or Finish on the last question, which opens T-R3.9d's Submit dialog
- [ ] Parts stay visible, following AGENTS' within-section deal. The strip has a gap between
      parts, and each square's accessible name includes its part title. The sheet shows part
      headings.
- [ ] Below 768, the footer holds Previous (icon only), a "4 / 8 · 3 answered" button and Next.
      The button opens a bottom sheet containing:
      - "Questions" and the answered count
      - a six-column grid of 44px squares under part headings
      - the legend: Answered, Flagged

      The sheet traps focus, closes on Esc or the backdrop, and returns focus.
- [ ] Flags keep `sessionStorage['quizzivy.flags.<attemptId>']`, and `flags.test.ts` is
      unchanged.
- [ ] Keys: ←/→ move, a–e choose, F flags, Esc closes. None fires in a text field or while a
      dialog is open (`keyboard.test.tsx` extended).
- [ ] The `studentNavigator` role leaves `useColumnWidth`, and
      `localStorage['quizzivy.column.studentNavigator']` is deleted once, inside try/catch. No
      `/app` route renders `SideColumn`, checked by a test over the router's student tree.

---

### T-R3.9d — Engine: integrity dialogs, submit and end states
**Depends on:** T-R3.9a
**Touches:** `web/src/features/integrity/components/{StrikeDialog,StrikeIndicator,FullscreenBar,AutoSubmitNotice}.tsx`, `web/src/features/take-test/components/ReviewScreen.tsx` (removed), `web/src/features/take-test/components/SubmitDialog.tsx` (new), `web/src/features/take-test/components/SubmittedScreen.tsx`, locales, `web/tests/units/integrity/{dialog,auto-submit,fullscreen}.test.tsx`, `web/tests/units/take-test/submit.test.ts`, `web/tests/e2e/{timer-expiry,integrity-away,session-takeover,audio-plays}.live.spec.ts`
**Size:** M
**Done when:**
- [ ] Focus loss shows an `alertdialog`, "You left the test", with the eye-off icon on a warning
      tile:
      - within the allowance: "That counts as n of m times allowed…"
      - past it under `flag`: "…Your teacher has been told. Your answers are safe." (D8)
      - the `warn`, −1 and `auto_submit` variants use the same frame; `auto_submit` keeps
        T-5.1's semantics

      The counting logic in `integrity/strikes.ts` is not touched.
- [ ] Copy or paste under `blockCopyPaste` shows the danger toast "Copy and paste are turned off
      in this test", with R1's error tone and a ban icon. The event is recorded as today.
- [ ] The return-to-fullscreen bar stays, in deck primitives.
- [ ] The Submit dialog shows:
      - the title "Submit with n unanswered?" or "Submit your test?"
      - Answered, Flagged and Time left
      - a "Go to" chip per unanswered question
      - Keep working and Submit test

      It flushes before submitting and stays idempotent against a racing auto-submit
      (`submit.test.ts`).
- [ ] Timer at zero, offline, session takeover (read-only), session expiry and the submitted
      screen keep today's behaviour in deck primitives. Each frame is listed for DG-80.
- [ ] `events_test.go`, `beacon.test.ts` and `buffer.test.ts` pass untouched.
- [ ] `pnpm e2e:live` passes E2E 5, 6, 7 and 8 with their assertions unchanged. Only locators
      follow the new UI.

---

### T-R3.10 — Result
**Depends on:** T-R3.4, T-R3.5
**Touches:** `web/src/features/results/pages/ResultPage.tsx`, `web/src/features/results/components/{ScoreRing,ResultTiles,ReviewItem}.tsx` (new), `web/src/features/results/api.ts`, locales, `web/tests/units/results/result.test.tsx`
**Size:** M
**Done when:**
- [ ] The column is at most 820 wide: a back link from 768, the detail header below 768.
- [ ] The summary card has:
      - a 112px ring: success from 80%, `--accent-c` from 60%, otherwise warning
      - the value with "of n" when the score is shown, or "…" while grading
      - class · submitted time, and the title
      - one sentence derived from the state: partly automatic, fully automatic, or pending
- [ ] Tiles:
      - while answers wait: Marked automatically (earned of auto points), Waiting for teacher
        (a count) and Time used
      - once graded, with `showScore`, on a paper of two or more parts: part tiles summed from
        `sections` and `sectionId`, two columns below 768 and up to four above
      - nothing when the score is withheld
- [ ] The filter offers All, Wrong and Waiting. Wrong appears only when scores are shown, and
      Waiting only when something waits. A lock line names the policy that hides whatever is
      hidden.
- [ ] Each review item shows:
      - a status circle, the number and the prompt, and the points
      - "You answered X", struck through in danger only when correctness is shown and wrong
      - "Correct answer Y", only with `showCorrectAnswers`
      - the explanation, only with `showExplanations`
      - the transcript, by its own rule
      - the grader's comment
- [ ] Variants: just submitted, fully automatic, and pending. The band variant is R6's. The
      deck's topic tiles (Result variant 2, a one-part grammar paper) are not built here; they
      have no model, and 76-r6.md T-R6.12 decides them (gap entry "Topic tiles have no
      model").
- [ ] Nothing renders from a field the policy removed. `result.test.tsx` covers every policy
      combination. Compared with the deck.

---

### T-R3.11 — Settings
**Depends on:** T-R3.5
**Touches:** `web/src/features/auth/pages/StudentSettingsPage.tsx`, `web/src/features/auth/components/SettingsSections.tsx`, `web/src/features/auth/{profileSchema,changePasswordSchema}.ts`, `web/src/features/auth/google/useGoogleSignIn.ts` (reused), locales, `web/tests/units/auth/settings-sections.test.tsx`, `web/tests/e2e/{settings,settings-navigation}.spec.ts`
**Size:** M
**Done when:**
- [ ] `/app/settings/:section` has four sections: Profile, Sign-in, Notifications (only with
      R4's notifications) and Appearance. Old section slugs redirect. The column is at most 760
      wide, and no nav item is lit.
- [ ] Profile:
      - an initials avatar; "Change photo" only once R4's avatar exists
      - full name through `PATCH /auth/me`, with "Your teacher sees this name."
      - email disabled, with "Ask your teacher to change your email."
      - Language (Tiếng Việt / English) through R1's locale, stored on the server once R4's
        preferences exist
- [ ] Sign-in:
      - the password row has no "Changed …" line yet; R5 T-R5.4 adds `passwordChangedAt` to
        `CurrentUser` (self only) and the line to this section, with its unit test
      - Change opens current and new password fields with a four-bar strength meter; the
        meter advises, R1's server rules decide
      - the Google row shows "Linked · email" with Unlink (only when `hasPassword`), or "Link
        Google" through the existing PKCE flow, never the GIS SDK
- [ ] Notifications, when shown: only the deck's switches whose events exist, with the deck's
      defaults. R4 brings "Test due soon" and "Result ready", R7 "Messages from teachers", R10
      "Daily practice reminder" (74-r4.md). Each switch covers both channels (DG-82).
- [ ] Appearance: Light, Dark and Device cards on R1's theme, and "Larger text in tests"
      (decision above).
- [ ] A form's state survives switching section and resizing across 768. Dirty and error states
      use R1 primitives. Compared with the deck.

---

### T-R3.12 — Student suites and the phone project
**Depends on:** T-R3.6 … T-R3.11
**Touches:** `web/playwright.config.ts` (a `mobile-chromium` project, Pixel 5), `web/package.json` (the `e2e` script), `web/tests/e2e/{smoke,student-layout,student-interactions,group-attempt,group-review}.spec.ts`, `web/tests/e2e/{answers-persist,payload-leak}.live.spec.ts`, `web/tests/units/ui/page-aside.test.tsx`, `web/tests/integration/router-chunks.test.ts`
**Size:** M
**Done when:**
- [ ] `mobile-chromium` runs the student specs at phone width: the bottom tabs, the detail
      header, the engine's sheet and the pane switcher.
      - its `testMatch` is limited to the student specs, and it has
        `testIgnore: /\.live\.spec\.ts$/`, as `chromium` does
      - `web/package.json`'s `e2e` becomes
        `playwright test --project=chromium --project=mobile-chromium`, so CI and local runs
        of `pnpm e2e` both execute it; the PR shows its tests in the run's output
- [ ] Every rewritten assertion states the same behaviour as before. The PR names each removed
      test, the behaviour it pinned, and why that behaviour left (the review screen, the
      navigator column). None is skipped.
- [ ] `router-chunks.test.ts` still shows the student tree and `FocusLayout` as lazy chunks
      outside the entry chunk. Its patterns are updated, not loosened.
- [ ] `pnpm e2e:live` passes E2E 2 and 9. E2E 9's key list is unchanged, because R3 adds no
      key-shaped field.
- [ ] The PR states the unit and E2E test counts before and after, and explains the difference.

---

### T-R3.13 — Browser verification against the deck
**Depends on:** T-R3.12
**Touches:** —
**Size:** M
**Done when:**
- [ ] Every `/app` route and the engine are measured as the seed student at 360, 768, 1024,
      1280 and 1440, in light and dark, beside `Quizzivy Student.dc.html` served on 5175, with
      a dev server confirmed fresh. `getBoundingClientRect` measures the 60px bars, the 52px
      options, the 34px squares, the 112px ring and each column's maximum width.
- [ ] The engine runs on the three-part paper (`seed/05-dev-sections.sql`) and on a passage
      group with a recording, and a phone run goes through the sheet and the pane switcher.
- [ ] The registered student skeleton is compared with `Quizzivy Splash.dc.html#skeleton` at
      the `destination` prop `student`, at 360 and 1280, light and dark. The teacher and admin
      destinations are compared by the releases that register them (T-R4.51, T-R5.33).
- [ ] Each deviation is fixed in this PR or recorded against DG-80 or DG-21 with its reason.
- [ ] Signed-in flows go through the Playwright route proxy with an API token. No password is
      typed into the pane.

---

### T-R3.14 — Docs
**Depends on:** T-R3.13
**Touches:** `AGENTS.md`, `docs/quizzivy-spec-v0.3.md` (§9, §10, §12, §15; version bumped per §18), `docs/plan/92-student-ux.md`, `docs/plan/30-risks.md`, `docs/plan/70-redesign-overview.md` §6, `docs/design/gaps.md`
**Size:** S
**Done when:**
- [ ] In AGENTS.md, the shipped rules replace the "Until R3" student paragraph:
      - the shell branches at 768 through a media query, with bottom tabs below it
      - detail routes declare `handle.detail`
      - page widths are 960, 720, 820 and 760, with container queries inside pages
      - the student side has no `PageAside` or `SideColumn`
      - the engine lives in `FocusLayout`: split panes from 768, the number strip and the phone
        sheet, with no navigator column and no width preference
- [ ] The "R3" bullet leaves "Redesign in progress" in AGENTS.md.
- [ ] In the spec, the "Availability: R3" markers go wherever the capability shipped, and
      anything built differently is corrected. The version is bumped.
- [ ] `92-student-ux.md` is marked superseded by the deck and this file.
- [ ] `30-risks.md` R-01 names the rebuilt engine's guards: the draft-compatibility test, the
      phone save strip and the unknown-type block.
- [ ] 70 §6 records that the student `quizzivy.column.*` keys were removed.
- [ ] Every new DG entry this file proposes is in `docs/design/gaps.md`, so the design team
      receives it: the result's attempt-level comment callout (open items).

---

## Release checklist

- [ ] Every task is ticked on `work/redesign-r3`. `develop` is merged into it, and verification
      is re-run after that merge.
- [ ] Every CI step runs locally, in order, judged by exit code (70 §8.1):
      - `make lint`, `make gen-check`
      - `make test-api` (after `docker compose up -d db minio`), and goose up/down/up
      - `pnpm lint`, `pnpm typecheck`, `pnpm format:check`
      - `pnpm test:unit`, `pnpm test:integration`, `pnpm build`
      - `pnpm e2e` (chromium and mobile-chromium), `pnpm e2e:content`
      - `pnpm e2e:live`, required because take-test, join and auth changed
      - `node scripts/check-design-deck.mjs`
- [ ] The five canaries pass, and their files are untouched: `git diff develop` shows nothing
      for them.
- [ ] Security: R2's isolation suite and escalation tests pass, E2E 9 passes, and no public
      operation was added.
- [ ] `drop_users_legacy_role` and `validate_ownership` are rehearsed on a Neon branch of
      production. The timings and the up/down/up run go in the release PR.
- [ ] The `legacy_admin_path` count is recorded (T-R3.3).
- [ ] Exam window: a query of production assignments open during the planned window returns
      none, or Thuong picks another window. Attempts in progress at deploy keep their answers:
      local drafts are read unchanged, and answers are keyed by question id.
- [ ] Deploy and smoke:
      - Thuong's go, then `release/0.9.0` → `main`
      - deploy the API (the release command migrates), then Pages
      - smoke `/healthz`, `/livez`, and sign-in as an admin and as a student
      - a student takes a short test on production in the owner's test class, once on a phone
        and once on a laptop
      - back-merge to `develop`, and tag `v0.9.0`
- [ ] Release notes, vi first:
      - a new student app that works at every screen size
      - tests in progress keep their answers
      - the check-before-submit page is now a dialog
      - nothing changes for teachers
      - old tabs are asked to reload
- [ ] T-R3.14's docs are in the release.

## Open items

- **DG-80.** These engine frames ship extrapolated from deck primitives and need the design
  team's sign-off: audio, image, fill-in, groups, parts, the timer at zero, the `warn` and
  `auto_submit` dialogs, fullscreen, offline, takeover, expiry, the unknown-type block and the
  phone save strip.
- **DG-82.** The Home badge (due within seven days) and the bell rules are interim.
- **DG-21.** Label widths in the number strip and the pills are sized to content with a minimum
  until the design team answers.
- **The result's comment callout has no model.** This is proposed as a new DG entry, which
  T-R3.14 adds to `docs/design/gaps.md`. A
  student-visible comment for the whole attempt would need a column and a grading control.
  Until the deck says which, only per-answer comments show (R6 covers the single-essay case).
- **Assignment note to students.** The deck's Test intro draws a teacher note, and no release
  had a field for it. Default: R4 builds it (`assignments.student_note`, `studentNote`) and
  T-R3.8 renders it. If Thuong decides not to build it, T-R3.8's note bullet is deleted and the
  element is recorded as not built.
- **Release order of R3 and R4** decides where the student bell is wired. The default is R3
  first (70 §2).
